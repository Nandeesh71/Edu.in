<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🤖</text></svg>">
    <title>Nandeesh S - AI/ML Engineer Portfolio</title>
    <style>
        /* ULTRA MAX PRO GLASSMORPHISM DESIGN SYSTEM */
        :root {
            /* Enhanced Glassmorphism Color Palette */
            --primary-blue: #007AFF;
            --secondary-blue: #5AC8FA;
            --dark-blue: #0051D5;
            --light-blue: #B3D9FF;
            --accent-blue: #4A90E2;
            --neon-blue: #00D4FF;
            --electric-blue: #0080FF;
            --cyber-blue: #00BFFF;
            
            /* Advanced Glassmorphism Backgrounds */
            --glass-white: rgba(255, 255, 255, 0.25);
            --glass-white-strong: rgba(255, 255, 255, 0.4);
            --glass-white-ultra: rgba(255, 255, 255, 0.15);
            --glass-white-max: rgba(255, 255, 255, 0.6);
            --glass-blue: rgba(0, 122, 255, 0.1);
            --glass-blue-strong: rgba(0, 122, 255, 0.2);
            --glass-blue-ultra: rgba(0, 122, 255, 0.05);
            --glass-blue-max: rgba(0, 122, 255, 0.3);
            --glass-dark: rgba(0, 0, 0, 0.1);
            --glass-dark-strong: rgba(0, 0, 0, 0.2);
            --glass-gradient: linear-gradient(135deg, rgba(255,255,255,0.2) 0%, rgba(255,255,255,0.05) 100%);
            
            /* Ultra Pro Max Glassmorphism Borders */
            --glass-border: rgba(255, 255, 255, 0.3);
            --glass-border-strong: rgba(255, 255, 255, 0.5);
            --glass-border-light: rgba(255, 255, 255, 0.2);
            --glass-border-blue: rgba(0, 122, 255, 0.3);
            --glass-border-neon: rgba(0, 212, 255, 0.4);
            --glass-border-electric: rgba(0, 128, 255, 0.5);
            
            /* Advanced Gradients */
            --gradient-primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --gradient-secondary: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            --gradient-tertiary: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            --gradient-glass: linear-gradient(135deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0.05) 100%);
            --gradient-mesh: radial-gradient(circle at 20% 80%, rgba(120, 119, 198, 0.3) 0%, transparent 50%),
                             radial-gradient(circle at 80% 20%, rgba(255, 119, 198, 0.3) 0%, transparent 50%),
                             radial-gradient(circle at 40% 40%, rgba(120, 219, 255, 0.3) 0%, transparent 50%);
            --gradient-aurora: linear-gradient(45deg, #667eea, #764ba2, #f093fb, #f5576c, #4facfe, #00f2fe);
            --gradient-cyber: linear-gradient(135deg, #00D4FF 0%, #007AFF 50%, #0051D5 100%);
            --gradient-neon: linear-gradient(135deg, #00BFFF 0%, #00D4FF 50%, #5AC8FA 100%);
            
            /* Typography */
            --font-primary: -apple-system, BlinkMacSystemFont, "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
            --font-display: "SF Pro Display", -apple-system, BlinkMacSystemFont, sans-serif;
            --font-mono: "SF Mono", Monaco, "Cascadia Code", monospace;
            
            /* Ultra Pro Max Shadows */
            --shadow-glass: 0 8px 32px rgba(31, 38, 135, 0.37);
            --shadow-glass-strong: 0 8px 32px rgba(31, 38, 135, 0.5);
            --shadow-glass-ultra: 0 12px 40px rgba(31, 38, 135, 0.6);
            --shadow-neon: 0 0 20px rgba(0, 212, 255, 0.5);
            --shadow-glow: 0 0 40px rgba(0, 122, 255, 0.3);
            --shadow-depth: 0 20px 40px rgba(0, 0, 0, 0.1);
            --shadow-electric: 0 0 30px rgba(0, 128, 255, 0.6);
            --shadow-cyber: 0 0 50px rgba(0, 191, 255, 0.4);
            --shadow-aurora: 0 0 60px rgba(102, 126, 234, 0.5);
            
            /* Advanced Transitions */
            --transition-glass: all 0.3s cubic-bezier(0.23, 1, 0.320, 1);
            --transition-smooth: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            --transition-bounce: all 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            --transition-elastic: all 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            --transition-ultra: all 0.5s cubic-bezier(0.165, 0.84, 0.44, 1);
            --transition-cyber: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            
            /* Spacing */
            --space-1: 0.25rem;
            --space-2: 0.5rem;
            --space-3: 0.75rem;
            --space-4: 1rem;
            --space-5: 1.25rem;
            --space-6: 1.5rem;
            --space-8: 2rem;
            --space-10: 2.5rem;
            --space-12: 3rem;
            --space-16: 4rem;
            --space-20: 5rem;
            --space-24: 6rem;
            --space-32: 8rem;
            
            /* Border Radius */
            --radius-sm: 8px;
            --radius-md: 12px;
            --radius-lg: 16px;
            --radius-xl: 24px;
            --radius-2xl: 32px;
            --radius-full: 50px;
            --radius-glass: 20px;
            --radius-ultra: 28px;
        }

        /* GLOBAL RESET & BASE STYLES */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        *::before,
        *::after {
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
            font-size: 16px;
            line-height: 1.5;
        }

        body {
            font-family: var(--font-primary);
            color: #1a1a1a;
            background: var(--gradient-mesh);
            background-attachment: fixed;
            line-height: 1.6;
            font-weight: 400;
            letter-spacing: -0.01em;
            overflow-x: hidden;
            min-height: 100vh;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            position: relative;
        }

        /* ULTRA PRO MAX GLASSMORPHISM UTILITIES */
        .glass {
            background: var(--glass-white);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border);
            border-radius: var(--radius-glass);
        }

        .glass-strong {
            background: var(--glass-white-strong);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            border: 1px solid var(--glass-border-strong);
        }

        .glass-ultra {
            background: var(--glass-white-ultra);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid var(--glass-border-light);
        }

        .glass-max {
            background: var(--glass-white-max);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border: 1px solid var(--glass-border-strong);
        }

        /* ANIMATED BACKGROUND WITH ENHANCED EFFECTS */
        .animated-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            background: var(--gradient-mesh);
            animation: meshMove 20s ease-in-out infinite;
        }

        .animated-bg::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--gradient-aurora);
            opacity: 0.1;
            animation: auroraMove 30s ease-in-out infinite;
        }

        .animated-bg::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 50% 50%, rgba(0, 122, 255, 0.1) 0%, transparent 70%);
            animation: pulseGlow 15s ease-in-out infinite;
        }

        @keyframes meshMove {
            0%, 100% { transform: translateX(0) translateY(0) rotate(0deg); }
            25% { transform: translateX(-20px) translateY(-20px) rotate(1deg); }
            50% { transform: translateX(20px) translateY(-10px) rotate(-1deg); }
            75% { transform: translateX(-10px) translateY(20px) rotate(0.5deg); }
        }

        @keyframes auroraMove {
            0%, 100% { transform: translateX(0) translateY(0) scale(1); }
            33% { transform: translateX(30px) translateY(-30px) scale(1.1); }
            66% { transform: translateX(-30px) translateY(30px) scale(0.9); }
        }

        @keyframes pulseGlow {
            0%, 100% { opacity: 0.1; transform: scale(1); }
            50% { opacity: 0.2; transform: scale(1.1); }
        }

        /* ULTRA PRO MAX GLASSMORPHISM HEADER */
        header {
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            padding: var(--space-4) 0;
            transition: var(--transition-glass);
            opacity: 0;
            transform: translateY(-100%);
            animation: slideDown 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards 0.3s;
        }

        .header-glass {
            background: var(--glass-white-max);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border: 1px solid var(--glass-border-strong);
            border-radius: var(--radius-2xl);
            margin: 0 var(--space-6);
            box-shadow: var(--shadow-glass-ultra);
            position: relative;
            overflow: hidden;
        }

        .header-glass::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: var(--transition-ultra);
        }

        .header-glass:hover::before {
            left: 100%;
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: var(--space-4) var(--space-8);
            position: relative;
            z-index: 2;
        }

        .nav-logo {
            font-family: var(--font-display);
            font-size: 26px;
            font-weight: 800;
            background: var(--gradient-cyber);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            cursor: pointer;
            transition: var(--transition-glass);
            opacity: 0;
            animation: fadeInUp 0.6s ease forwards 0.5s;
            position: relative;
        }

        .nav-logo::after {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--gradient-neon);
            transition: var(--transition-glass);
        }

        .nav-logo:hover {
            transform: scale(1.05);
            filter: drop-shadow(0 0 15px rgba(0, 122, 255, 0.6));
        }

        .nav-logo:hover::after {
            width: 100%;
        }

        .nav-links {
            display: flex;
            gap: var(--space-8);
            align-items: center;
        }

        .nav-links a {
            color: #333;
            text-decoration: none;
            font-size: 17px;
            font-weight: 600;
            transition: var(--transition-glass);
            opacity: 0;
            animation: fadeInUp 0.6s ease forwards;
            position: relative;
            padding: var(--space-3) var(--space-5);
            border-radius: var(--radius-lg);
            overflow: hidden;
        }

        .nav-links a:nth-child(1) { animation-delay: 0.6s; }
        .nav-links a:nth-child(2) { animation-delay: 0.7s; }
        .nav-links a:nth-child(3) { animation-delay: 0.8s; }
        .nav-links a:nth-child(4) { animation-delay: 0.9s; }

        .nav-links a::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--glass-blue-max);
            border-radius: var(--radius-lg);
            opacity: 0;
            transition: var(--transition-glass);
            z-index: -1;
            transform: scale(0.8);
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: radial-gradient(circle, rgba(0, 122, 255, 0.3) 0%, transparent 70%);
            transition: var(--transition-bounce);
            transform: translate(-50%, -50%);
            border-radius: 50%;
        }

        .nav-links a:hover::before {
            opacity: 1;
            transform: scale(1);
        }

        .nav-links a:hover::after {
            width: 100px;
            height: 100px;
        }

        .nav-links a:hover {
            color: var(--primary-blue);
            transform: translateY(-3px);
            text-shadow: 0 0 10px rgba(0, 122, 255, 0.5);
        }

        /* ULTRA PRO MAX GLASSMORPHISM HERO SECTION */
        .hero {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            text-align: center;
            padding: 0 var(--space-6);
            position: relative;
            overflow: hidden;
        }

        .hero-glass {
            background: var(--glass-white-max);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border: 1px solid var(--glass-border-strong);
            border-radius: var(--radius-2xl);
            padding: var(--space-20);
            box-shadow: var(--shadow-glass-ultra);
            max-width: 1000px;
            margin: 0 auto;
            position: relative;
            overflow: hidden;
        }

        .hero-glass::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: conic-gradient(from 0deg, transparent, rgba(0, 122, 255, 0.1), transparent);
            animation: rotate 20s linear infinite;
            z-index: -1;
        }

        .hero-glass::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--glass-gradient);
            z-index: -1;
        }

        @keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        .hero h1 {
            font-family: var(--font-display);
            font-size: clamp(52px, 7vw, 84px);
            font-weight: 900;
            letter-spacing: -0.03em;
            margin-bottom: var(--space-8);
            background: var(--gradient-cyber);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            line-height: 1.1;
            opacity: 0;
            transform: translateY(40px);
            animation: fadeInUp 0.8s ease forwards 1s;
            position: relative;
        }

        .hero h1::after {
            content: '';
            position: absolute;
            bottom: -8px;
            left: 50%;
            transform: translateX(-50%);
            width: 0;
            height: 4px;
            background: var(--gradient-neon);
            border-radius: var(--radius-sm);
            animation: expandLine 1s ease forwards 1.8s;
        }

        @keyframes expandLine {
            to { width: 200px; }
        }

        .hero h2 {
            font-size: clamp(22px, 4vw, 32px);
            font-weight: 500;
            color: #555;
            margin-bottom: var(--space-16);
            line-height: 1.4;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.8s ease forwards 1.2s;
        }

        .cta-button {
            display: inline-flex;
            align-items: center;
            gap: var(--space-3);
            background: var(--glass-blue-max);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            color: var(--primary-blue);
            padding: var(--space-6) var(--space-12);
            border-radius: var(--radius-full);
            text-decoration: none;
            font-size: 20px;
            font-weight: 700;
            transition: var(--transition-glass);
            border: 2px solid var(--glass-border-blue);
            box-shadow: var(--shadow-glass-strong);
            opacity: 0;
            transform: translateY(30px) scale(0.9);
            animation: fadeInUp 0.8s ease forwards 1.4s;
            position: relative;
            overflow: hidden;
        }

        .cta-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: var(--transition-ultra);
        }

        .cta-button:hover::before {
            left: 100%;
        }

        .cta-button:hover {
            transform: translateY(-6px) scale(1.05);
            box-shadow: var(--shadow-electric);
            background: var(--primary-blue);
            color: white;
            border-color: var(--neon-blue);
        }

        /* ENHANCED FLOATING PARTICLES */
        .particles {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: -1;
        }

        .particle {
            position: absolute;
            background: var(--glass-white-strong);
            border-radius: 50%;
            animation: float 8s ease-in-out infinite;
            box-shadow: var(--shadow-glow);
        }

        .particle:nth-child(1) {
            width: 100px;
            height: 100px;
            left: 10%;
            animation-delay: 0s;
            background: radial-gradient(circle, rgba(0, 122, 255, 0.3) 0%, transparent 70%);
        }

        .particle:nth-child(2) {
            width: 80px;
            height: 80px;
            left: 20%;
            animation-delay: 2s;
            background: radial-gradient(circle, rgba(0, 212, 255, 0.3) 0%, transparent 70%);
        }

        .particle:nth-child(3) {
            width: 60px;
            height: 60px;
            left: 70%;
            animation-delay: 4s;
            background: radial-gradient(circle, rgba(90, 200, 250, 0.3) 0%, transparent 70%);
        }

        .particle:nth-child(4) {
            width: 120px;
            height: 120px;
            left: 80%;
            animation-delay: 6s;
            background: radial-gradient(circle, rgba(0, 128, 255, 0.2) 0%, transparent 70%);
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg) scale(1); }
            25% { transform: translateY(-30px) rotate(90deg) scale(1.1); }
            50% { transform: translateY(-20px) rotate(180deg) scale(0.9); }
            75% { transform: translateY(-40px) rotate(270deg) scale(1.05); }
        }

        /* ULTRA PRO MAX GLASSMORPHISM SECTIONS */
        .section {
            padding: var(--space-32) var(--space-6);
            max-width: 1400px;
            margin: 0 auto;
            position: relative;
        }

        .section-glass {
            background: var(--glass-white-max);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border: 1px solid var(--glass-border-strong);
            border-radius: var(--radius-2xl);
            padding: var(--space-20);
            margin: var(--space-20) 0;
            box-shadow: var(--shadow-glass-ultra);
            position: relative;
            overflow: hidden;
        }

        .section-glass::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: var(--gradient-neon);
            transform: scaleX(0);
            transition: var(--transition-ultra);
        }

        .section-glass:hover::before {
            transform: scaleX(1);
        }

        .section-title {
            text-align: center;
            font-family: var(--font-display);
            font-size: clamp(40px, 6vw, 64px);
            font-weight: 900;
            margin-bottom: var(--space-20);
            background: var(--gradient-cyber);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            letter-spacing: -0.03em;
            opacity: 0;
            transform: translateY(40px);
            transition: var(--transition-smooth);
            position: relative;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -12px;
            left: 50%;
            transform: translateX(-50%);
            width: 120px;
            height: 4px;
            background: var(--gradient-neon);
            border-radius: var(--radius-sm);
            opacity: 0;
            transition: var(--transition-smooth);
        }

        .section-title.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .section-title.visible::after {
            opacity: 1;
        }

        /* ULTRA INTERACTIVE WORK GRID */
        .work-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
            gap: var(--space-10);
            margin-top: var(--space-20);
        }

        .work-item {
            background: var(--glass-white-strong);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            border: 1px solid var(--glass-border);
            border-radius: var(--radius-2xl);
            overflow: hidden;
            transition: var(--transition-glass);
            box-shadow: var(--shadow-glass);
            opacity: 0;
            transform: translateY(40px);
            animation: fadeInUp 0.8s ease forwards;
            cursor: pointer;
            position: relative;
            group: work-item;
        }

        .work-item:nth-child(1) { animation-delay: 0.1s; }
        .work-item:nth-child(2) { animation-delay: 0.2s; }
        .work-item:nth-child(3) { animation-delay: 0.3s; }
        .work-item:nth-child(4) { animation-delay: 0.4s; }
        .work-item:nth-child(5) { animation-delay: 0.5s; }
        .work-item:nth-child(6) { animation-delay: 0.6s; }

        .work-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--gradient-glass);
            opacity: 0;
            transition: var(--transition-glass);
            z-index: 1;
        }

        .work-item::after {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: var(--gradient-neon);
            border-radius: var(--radius-2xl);
            opacity: 0;
            transition: var(--transition-glass);
            z-index: -1;
        }

        .work-item:hover::before {
            opacity: 1;
        }

        .work-item:hover::after {
            opacity: 1;
        }

        .work-item:hover {
            transform: translateY(-16px) scale(1.03);
            box-shadow: var(--shadow-electric);
            border-color: var(--neon-blue);
        }

        .work-image {
            width: 100%;
            height: 280px;
            object-fit: cover;
            transition: var(--transition-glass);
            position: relative;
        }

        .work-item:hover .work-image {
            transform: scale(1.1);
            filter: brightness(1.2) saturate(1.1);
        }

        .work-content {
            padding: var(--space-10);
            position: relative;
            z-index: 2;
        }

        .work-content h3 {
            font-size: 26px;
            font-weight: 800;
            margin-bottom: var(--space-5);
            color: #222;
            line-height: 1.3;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .work-content p {
            color: #666;
            margin-bottom: var(--space-8);
            line-height: 1.7;
            font-size: 17px;
        }

        .work-link {
            display: inline-flex;
            align-items: center;
            gap: var(--space-3);
            background: var(--glass-blue-strong);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            color: var(--primary-blue);
            padding: var(--space-4) var(--space-8);
            border-radius: var(--radius-full);
            text-decoration: none;
            font-weight: 700;
            transition: var(--transition-glass);
            border: 1px solid var(--glass-border-blue);
            position: relative;
            overflow: hidden;
        }

        .work-link::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: var(--transition-ultra);
        }

        .work-link:hover::before {
            left: 100%;
        }

        .work-link:hover {
            background: var(--primary-blue);
            color: white;
            transform: translateX(6px);
            box-shadow: var(--shadow-neon);
        }

        /* ULTRA PRO MAX GLASSMORPHISM ABOUT SECTION */
        .about-content {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: var(--space-20);
            align-items: center;
        }

        .profile-container {
            position: relative;
        }

        .profile-image {
            width: 360px;
            height: 360px;
            border-radius: var(--radius-2xl);
            object-fit: cover;
            transition: var(--transition-glass);
            border: 3px solid var(--glass-border-strong);
            box-shadow: var(--shadow-glass-ultra);
            position: relative;
            z-index: 2;
        }

        .profile-image:hover {
            transform: scale(1.05) rotate(3deg);
            box-shadow: var(--shadow-electric);
        }

        .profile-glow {
            position: absolute;
            top: -30px;
            left: -30px;
            right: -30px;
            bottom: -30px;
            background: var(--gradient-cyber);
            border-radius: var(--radius-2xl);
            opacity: 0;
            transition: var(--transition-glass);
            z-index: -1;
            filter: blur(30px);
        }

        .profile-container:hover .profile-glow {
            opacity: 0.4;
        }

        .profile-container::before {
            content: '';
            position: absolute;
            top: -40px;
            left: -40px;
            right: -40px;
            bottom: -40px;
            background: conic-gradient(from 0deg, var(--neon-blue), var(--primary-blue), var(--electric-blue), var(--neon-blue));
            border-radius: var(--radius-2xl);
            opacity: 0;
            transition: var(--transition-glass);
            z-index: -2;
            animation: rotate 10s linear infinite;
        }

        .profile-container:hover::before {
            opacity: 0.3;
        }

        .about-text h2 {
            font-size: 52px;
            font-weight: 900;
            margin-bottom: var(--space-10);
            background: var(--gradient-cyber);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            letter-spacing: -0.03em;
            line-height: 1.2;
        }

        .about-text p {
            font-size: 19px;
            line-height: 1.8;
            color: #555;
            margin-bottom: var(--space-8);
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.8s ease forwards;
        }

        .about-text p:nth-child(2) { animation-delay: 0.1s; }
        .about-text p:nth-child(3) { animation-delay: 0.2s; }
        .about-text p:nth-child(4) { animation-delay: 0.3s; }
        .about-text p:nth-child(5) { animation-delay: 0.4s; }
        .about-text p:nth-child(6) { animation-delay: 0.5s; }

        /* ULTRA INTERACTIVE SKILLS SECTION */
        .skills-category {
            margin-bottom: var(--space-24);
        }

        .skills-category h3 {
            font-size: 42px;
            font-weight: 900;
            color: #333;
            text-align: center;
            margin-bottom: var(--space-16);
            position: relative;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .skills-category h3::after {
            content: '';
            position: absolute;
            bottom: -16px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 5px;
            background: var(--gradient-neon);
            border-radius: var(--radius-sm);
        }

        /* ULTRA INTERACTIVE PROGRAMMING GRID */
        .programming-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: var(--space-8);
            margin-bottom: var(--space-20);
        }

        .programming-item {
            background: var(--glass-white-strong);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            border: 1px solid var(--glass-border);
            border-radius: var(--radius-2xl);
            padding: var(--space-10);
            text-align: center;
            transition: var(--transition-glass);
            cursor: pointer;
            position: relative;
            overflow: hidden;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.6s ease forwards;
        }

        .programming-item:nth-child(1) { animation-delay: 0.1s; }
        .programming-item:nth-child(2) { animation-delay: 0.2s; }
        .programming-item:nth-child(3) { animation-delay: 0.3s; }
        .programming-item:nth-child(4) { animation-delay: 0.4s; }
        .programming-item:nth-child(5) { animation-delay: 0.5s; }
        .programming-item:nth-child(6) { animation-delay: 0.6s; }
        .programming-item:nth-child(7) { animation-delay: 0.7s; }

        .programming-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(0, 122, 255, 0.2), transparent);
            transition: var(--transition-ultra);
        }

        .programming-item::after {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: var(--gradient-neon);
            border-radius: var(--radius-2xl);
            opacity: 0;
            transition: var(--transition-glass);
            z-index: -1;
        }

        .programming-item:hover::before {
            left: 100%;
        }

        .programming-item:hover::after {
            opacity: 1;
        }

        .programming-item:hover {
            transform: translateY(-12px) scale(1.08);
            box-shadow: var(--shadow-electric);
            border-color: var(--neon-blue);
        }

        .programming-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto var(--space-6);
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center;
            transition: var(--transition-glass);
            filter: drop-shadow(0 6px 12px rgba(0,0,0,0.15));
        }

        .programming-item:hover .programming-icon {
            transform: scale(1.3) rotate(10deg);
            filter: drop-shadow(0 12px 24px rgba(0,122,255,0.4));
        }

        .programming-name {
            font-size: 20px;
            font-weight: 700;
            color: #333;
            margin-bottom: var(--space-4);
        }

        .skill-level {
            width: 100%;
            height: 8px;
            background: var(--glass-white-ultra);
            border-radius: var(--radius-sm);
            overflow: hidden;
            margin-top: var(--space-4);
            position: relative;
        }

        .skill-progress {
            height: 100%;
            background: var(--gradient-neon);
            border-radius: var(--radius-sm);
            transition: width 1.5s cubic-bezier(0.4, 0, 0.2, 1);
            width: 0;
            position: relative;
        }

        .skill-progress::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.5), transparent);
            animation: shimmer 2s infinite;
        }

        @keyframes shimmer {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }

        .programming-item:hover .skill-progress {
            width: var(--skill-width);
        }

        .skill-percentage {
            position: absolute;
            top: -30px;
            right: 0;
            font-size: 14px;
            font-weight: 600;
            color: var(--primary-blue);
            opacity: 0;
            transition: var(--transition-glass);
        }

        .programming-item:hover .skill-percentage {
            opacity: 1;
        }

        /* FRAMEWORK LOGOS */
        .python-icon { background-image: url('https://cdn.jsdelivr.net/gh/devicons/devicon/icons/python/python-original.svg'); }
        .r-icon { background-image: url('https://cdn.jsdelivr.net/gh/devicons/devicon/icons/r/r-original.svg'); }
        .java-icon { background-image: url('https://cdn.jsdelivr.net/gh/devicons/devicon/icons/java/java-original.svg'); }
        .cpp-icon { background-image: url('https://cdn.jsdelivr.net/gh/devicons/devicon/icons/cplusplus/cplusplus-original.svg'); }
        .html-icon { background-image: url('https://cdn.jsdelivr.net/gh/devicons/devicon/icons/html5/html5-original.svg'); }
        .css-icon { background-image: url('https://cdn.jsdelivr.net/gh/devicons/devicon/icons/css3/css3-original.svg'); }
        .js-icon { background-image: url('https://cdn.jsdelivr.net/gh/devicons/devicon/icons/javascript/javascript-original.svg'); }

        /* ULTRA FRAMEWORKS GRID */
        .frameworks-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: var(--space-8);
            margin-bottom: var(--space-20);
        }

        .framework-item {
            background: var(--glass-blue-ultra);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border-blue);
            border-radius: var(--radius-2xl);
            padding: var(--space-10);
            text-align: center;
            transition: var(--transition-glass);
            cursor: pointer;
            position: relative;
            overflow: hidden;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.6s ease forwards;
        }

        .framework-item:nth-child(1) { animation-delay: 0.1s; }
        .framework-item:nth-child(2) { animation-delay: 0.2s; }
        .framework-item:nth-child(3) { animation-delay: 0.3s; }
        .framework-item:nth-child(4) { animation-delay: 0.4s; }
        .framework-item:nth-child(5) { animation-delay: 0.5s; }
        .framework-item:nth-child(6) { animation-delay: 0.6s; }

        .framework-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--gradient-glass);
            opacity: 0;
            transition: var(--transition-glass);
        }

        .framework-item:hover::before {
            opacity: 1;
        }

        .framework-item:hover {
            transform: translateY(-12px) scale(1.08);
            box-shadow: var(--shadow-electric);
            background: var(--glass-blue-strong);
            border-color: var(--neon-blue);
        }

        .framework-icon {
            width: 90px;
            height: 90px;
            margin: 0 auto var(--space-6);
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center;
            transition: var(--transition-glass);
            filter: drop-shadow(0 6px 12px rgba(0,0,0,0.15));
        }

        .framework-item:hover .framework-icon {
            transform: scale(1.3) rotate(-10deg);
            filter: drop-shadow(0 12px 24px rgba(0,122,255,0.4));
        }

        .framework-name {
            font-size: 20px;
            font-weight: 700;
            color: #333;
        }

        /* FRAMEWORK LOGOS */
        .sklearn-icon { background-image: url('https://upload.wikimedia.org/wikipedia/commons/0/05/Scikit_learn_logo_small.svg'); }
        .tensorflow-icon { background-image: url('https://cdn.jsdelivr.net/gh/devicons/devicon/icons/tensorflow/tensorflow-original.svg'); }
        .pytorch-icon { background-image: url('https://cdn.jsdelivr.net/gh/devicons/devicon/icons/pytorch/pytorch-original.svg'); }
        .numpy-icon { background-image: url('https://cdn.jsdelivr.net/gh/devicons/devicon/icons/numpy/numpy-original.svg'); }
        .matplotlib-icon { background-image: url('https://matplotlib.org/stable/_images/sphx_glr_logos2_003.png'); }
        .pandas-icon { background-image: url('https://cdn.jsdelivr.net/gh/devicons/devicon/icons/pandas/pandas-original.svg'); }

        /* ULTRA EXPERTISE AREAS */
        .expertise-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: var(--space-10);
            margin-bottom: var(--space-20);
        }

        .expertise-item {
            background: var(--glass-white-strong);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            border: 1px solid var(--glass-border);
            border-radius: var(--radius-2xl);
            padding: var(--space-10);
            transition: var(--transition-glass);
            cursor: pointer;
            position: relative;
            overflow: hidden;
            opacity: 0;
            transform: translateX(-30px);
            animation: slideInLeft 0.8s ease forwards;
        }

        .expertise-item:nth-child(1) { animation-delay: 0.1s; }
        .expertise-item:nth-child(2) { animation-delay: 0.2s; }
        .expertise-item:nth-child(3) { animation-delay: 0.3s; }
        .expertise-item:nth-child(4) { animation-delay: 0.4s; }

        .expertise-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 6px;
            height: 100%;
            background: var(--gradient-neon);
            transform: scaleY(0);
            transition: var(--transition-glass);
        }

        .expertise-item::after {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: var(--gradient-cyber);
            border-radius: var(--radius-2xl);
            opacity: 0;
            transition: var(--transition-glass);
            z-index: -1;
        }

        .expertise-item:hover::before {
            transform: scaleY(1);
        }

        .expertise-item:hover::after {
            opacity: 0.3;
        }

        .expertise-item:hover {
            transform: translateX(12px) scale(1.03);
            box-shadow: var(--shadow-electric);
            border-color: var(--neon-blue);
        }

        .expertise-header {
            display: flex;
            align-items: center;
            margin-bottom: var(--space-6);
        }

        .expertise-icon {
            width: 100px;
            height: 100px;
            margin-right: var(--space-6);
            border-radius: var(--radius-xl);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 42px;
            background: var(--glass-blue-strong);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border-blue);
            transition: var(--transition-glass);
            position: relative;
            overflow: hidden;
        }

        .expertise-icon::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: var(--transition-ultra);
        }

        .expertise-item:hover .expertise-icon::before {
            left: 100%;
        }

        .expertise-item:hover .expertise-icon {
            background: var(--primary-blue);
            color: white;
            transform: scale(1.15) rotate(10deg);
            box-shadow: var(--shadow-neon);
        }

        .expertise-name {
            font-size: 26px;
            font-weight: 800;
            color: #333;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .expertise-description {
            color: #666;
            line-height: 1.7;
            font-size: 17px;
        }

        /* ULTRA INTERACTIVE CONTACT SECTION */
        .contact-section-intro {
            text-align: center;
            max-width: 900px;
            margin: 0 auto var(--space-20);
        }

        .contact-section-intro p {
            font-size: 24px;
            line-height: 1.8;
            color: #555;
            margin-bottom: var(--space-10);
        }

        .contact-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: var(--space-10);
            margin-bottom: var(--space-20);
        }

        .contact-card {
            background: var(--glass-white-strong);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border: 1px solid var(--glass-border);
            border-radius: var(--radius-2xl);
            padding: var(--space-12);
            text-align: center;
            transition: var(--transition-glass);
            cursor: pointer;
            position: relative;
            overflow: hidden;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.8s ease forwards;
        }

        .contact-card:nth-child(1) { animation-delay: 0.1s; }
        .contact-card:nth-child(2) { animation-delay: 0.2s; }
        .contact-card:nth-child(3) { animation-delay: 0.3s; }

        .contact-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--gradient-glass);
            opacity: 0;
            transition: var(--transition-glass);
        }

        .contact-card::after {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: var(--gradient-neon);
            border-radius: var(--radius-2xl);
            opacity: 0;
            transition: var(--transition-glass);
            z-index: -1;
        }

        .contact-card:hover::before {
            opacity: 1;
        }

        .contact-card:hover::after {
            opacity: 1;
        }

        .contact-card:hover {
            transform: translateY(-12px) scale(1.05);
            box-shadow: var(--shadow-electric);
            border-color: var(--neon-blue);
        }

        .contact-icon-large {
            width: 100px;
            height: 100px;
            margin: 0 auto var(--space-6);
            background: var(--glass-blue-strong);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border-blue);
            border-radius: var(--radius-xl);
            display: flex;
            align-items: center;
            justify-content: center;
            transition: var(--transition-glass);
            position: relative;
            z-index: 2;
            overflow: hidden;
        }

        .contact-icon-large::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: var(--transition-ultra);
        }

        .contact-card:hover .contact-icon-large::before {
            left: 100%;
        }

        .contact-card:hover .contact-icon-large {
            background: var(--primary-blue);
            transform: scale(1.15) rotate(10deg);
            box-shadow: var(--shadow-neon);
        }

        .contact-icon-large svg {
            width: 50px;
            height: 50px;
            stroke: var(--primary-blue);
            transition: var(--transition-glass);
        }

        .contact-card:hover .contact-icon-large svg {
            stroke: white;
            transform: scale(1.1);
        }

        .contact-title {
            font-size: 28px;
            font-weight: 800;
            color: #333;
            margin-bottom: var(--space-4);
            position: relative;
            z-index: 2;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .contact-info {
            color: #666;
            font-size: 17px;
            position: relative;
            z-index: 2;
            line-height: 1.6;
        }

        /* ULTRA PRO MAX GLASSMORPHISM CHATBOT */
        .chatbot-container {
            position: fixed;
            bottom: var(--space-10);
            right: var(--space-10);
            z-index: 3000;
        }

        .chatbot-toggle {
            width: 90px;
            height: 90px;
            border-radius: 50%;
            background: var(--glass-blue-max);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border: 2px solid var(--glass-border-blue);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: var(--shadow-glass-ultra);
            transition: var(--transition-glass);
            position: relative;
            overflow: hidden;
        }

        .chatbot-toggle::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--gradient-cyber);
            opacity: 0;
            transition: var(--transition-glass);
        }

        .chatbot-toggle::after {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: conic-gradient(from 0deg, transparent, rgba(0, 122, 255, 0.3), transparent);
            animation: rotate 8s linear infinite;
            opacity: 0;
            transition: var(--transition-glass);
        }

        .chatbot-toggle:hover::before {
            opacity: 0.9;
        }

        .chatbot-toggle:hover::after {
            opacity: 1;
        }

        .chatbot-toggle:hover {
            transform: scale(1.15);
            box-shadow: var(--shadow-electric);
        }

        .chatbot-icon {
            width: 42px;
            height: 42px;
            stroke: var(--primary-blue);
            transition: var(--transition-glass);
            position: relative;
            z-index: 2;
        }

        .chatbot-toggle:hover .chatbot-icon {
            stroke: white;
            transform: scale(1.2);
        }

        .notification-badge {
            position: absolute;
            top: -10px;
            right: -10px;
            width: 32px;
            height: 32px;
            background: #FF3B30;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
            font-weight: 800;
            color: white;
            opacity: 0;
            transform: scale(0);
            transition: var(--transition-bounce);
            box-shadow: var(--shadow-glass);
            border: 2px solid white;
        }

        .notification-badge.active {
            opacity: 1;
            transform: scale(1);
            animation: bounce 0.8s ease;
        }

        @keyframes bounce {
            0%, 20%, 53%, 80%, 100% { transform: scale(1); }
            40%, 43% { transform: scale(1.3); }
            70% { transform: scale(1.15); }
        }

        .chatbot-window {
            position: absolute;
            bottom: 110px;
            right: 0;
            width: 450px;
            height: 700px;
            background: var(--glass-white-max);
            backdrop-filter: blur(35px);
            -webkit-backdrop-filter: blur(35px);
            border: 1px solid var(--glass-border-strong);
            border-radius: var(--radius-2xl);
            box-shadow: var(--shadow-glass-ultra);
            opacity: 0;
            transform: translateY(30px) scale(0.9);
            transition: var(--transition-glass);
            pointer-events: none;
            overflow: hidden;
        }

        .chatbot-window.active {
            opacity: 1;
            transform: translateY(0) scale(1);
            pointer-events: all;
        }

        .chatbot-window::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: var(--gradient-neon);
        }

        .chatbot-header {
            padding: var(--space-8);
            border-bottom: 1px solid var(--glass-border);
            background: var(--glass-blue-ultra);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            position: relative;
        }

        .chatbot-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(0, 122, 255, 0.1), transparent);
            animation: headerShimmer 3s infinite;
        }

        @keyframes headerShimmer {
            0% { left: -100%; }
            100% { left: 100%; }
        }

        .chatbot-header h3 {
            font-size: 22px;
            font-weight: 800;
            color: #333;
            margin-bottom: var(--space-3);
            background: var(--gradient-cyber);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .chatbot-status {
            font-size: 15px;
            color: #666;
            display: flex;
            align-items: center;
            gap: var(--space-3);
            font-weight: 600;
        }

        .status-dot {
            width: 12px;
            height: 12px;
            background: #34C759;
            border-radius: 50%;
            animation: statusPulse 2s infinite;
            box-shadow: 0 0 15px rgba(52, 199, 89, 0.6);
        }

        @keyframes statusPulse {
            0%, 100% { opacity: 1; transform: scale(1); }
            50% { opacity: 0.7; transform: scale(1.2); }
        }

        .chatbot-messages {
            height: 480px;
            overflow-y: auto;
            padding: var(--space-6);
            display: flex;
            flex-direction: column;
            gap: var(--space-5);
        }

        .chatbot-messages::-webkit-scrollbar {
            width: 8px;
        }

        .chatbot-messages::-webkit-scrollbar-track {
            background: var(--glass-white-ultra);
            border-radius: var(--radius-sm);
        }

        .chatbot-messages::-webkit-scrollbar-thumb {
            background: var(--glass-blue-strong);
            border-radius: var(--radius-sm);
            border: 1px solid var(--glass-border-blue);
        }

        .chatbot-messages::-webkit-scrollbar-thumb:hover {
            background: var(--primary-blue);
        }

        .message {
            max-width: 85%;
            padding: var(--space-5) var(--space-6);
            border-radius: var(--radius-xl);
            font-size: 16px;
            line-height: 1.6;
            animation: messageSlide 0.5s ease;
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border-light);
            position: relative;
            overflow: hidden;
        }

        .message::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: var(--transition-ultra);
        }

        .message:hover::before {
            left: 100%;
        }

        @keyframes messageSlide {
            from {
                opacity: 0;
                transform: translateY(20px) scale(0.95);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        .message.user {
            align-self: flex-end;
            background: var(--glass-blue-max);
            color: var(--primary-blue);
            border-bottom-right-radius: 8px;
            border-color: var(--glass-border-blue);
        }

        .message.bot {
            align-self: flex-start;
            background: var(--glass-white-strong);
            color: #333;
            border-bottom-left-radius: 8px;
        }

        .quick-options {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: var(--space-4);
            margin: var(--space-6) 0;
        }

        .quick-option {
            background: var(--glass-white-strong);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border);
            border-radius: var(--radius-lg);
            padding: var(--space-4);
            color: #333;
            font-size: 15px;
            font-weight: 700;
            cursor: pointer;
            transition: var(--transition-glass);
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .quick-option::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(0, 122, 255, 0.2), transparent);
            transition: var(--transition-ultra);
        }

        .quick-option:hover::before {
            left: 100%;
        }

        .quick-option:hover {
            background: var(--primary-blue);
            color: white;
            transform: translateY(-3px);
            box-shadow: var(--shadow-neon);
            border-color: var(--neon-blue);
        }

        .typing-indicator {
            align-self: flex-start;
            padding: var(--space-5) var(--space-6);
            background: var(--glass-white-strong);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border-light);
            border-radius: var(--radius-xl);
            border-bottom-left-radius: 8px;
            opacity: 0;
            transform: translateY(20px);
            transition: var(--transition-glass);
        }

        .typing-indicator.active {
            opacity: 1;
            transform: translateY(0);
        }

        .typing-dots {
            display: flex;
            gap: 8px;
            align-items: center;
        }

        .typing-dot {
            width: 12px;
            height: 12px;
            background: var(--primary-blue);
            border-radius: 50%;
            animation: typingDot 1.6s infinite ease-in-out;
            box-shadow: 0 0 10px rgba(0, 122, 255, 0.5);
        }

        .typing-dot:nth-child(1) { animation-delay: 0s; }
        .typing-dot:nth-child(2) { animation-delay: 0.3s; }
        .typing-dot:nth-child(3) { animation-delay: 0.6s; }

        @keyframes typingDot {
            0%, 60%, 100% { transform: scale(1); opacity: 0.5; }
            30% { transform: scale(1.4); opacity: 1; }
        }

        .chatbot-input-area {
            padding: var(--space-6);
            border-top: 1px solid var(--glass-border);
            background: var(--glass-blue-ultra);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
        }

        .chatbot-input-container {
            display: flex;
            gap: var(--space-4);
            align-items: center;
        }

        .chatbot-input {
            flex: 1;
            padding: var(--space-5);
            border: 1px solid var(--glass-border);
            border-radius: var(--radius-xl);
            background: var(--glass-white-strong);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            color: #333;
            font-size: 16px;
            outline: none;
            transition: var(--transition-glass);
            font-weight: 500;
        }

        .chatbot-input:focus {
            border-color: var(--primary-blue);
            box-shadow: 0 0 0 4px rgba(0, 122, 255, 0.2);
            background: var(--glass-white-max);
        }

        .chatbot-input::placeholder {
            color: #999;
            font-weight: 400;
        }

        .chatbot-send-btn {
            width: 56px;
            height: 56px;
            border: none;
            border-radius: var(--radius-xl);
            background: var(--primary-blue);
            color: white;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: var(--transition-glass);
            box-shadow: var(--shadow-glass);
            position: relative;
            overflow: hidden;
        }

        .chatbot-send-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: var(--transition-ultra);
        }

        .chatbot-send-btn:hover::before {
            left: 100%;
        }

        .chatbot-send-btn:hover {
            background: var(--dark-blue);
            transform: scale(1.08);
            box-shadow: var(--shadow-neon);
        }

        /* RESPONSIVE DESIGN */
        @media (max-width: 1024px) {
            .about-content {
                grid-template-columns: 1fr;
                text-align: center;
                gap: var(--space-16);
            }

            .frameworks-grid {
                grid-template-columns: repeat(3, 1fr);
            }

            .contact-grid {
                grid-template-columns: 1fr;
            }

            .expertise-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            .hero h1 {
                font-size: clamp(40px, 9vw, 52px);
            }

            .hero h2 {
                font-size: clamp(20px, 5vw, 28px);
            }

            .section {
                padding: var(--space-20) var(--space-4);
            }

            .section-glass {
                padding: var(--space-12);
                margin: var(--space-12) 0;
            }

            .work-grid {
                grid-template-columns: 1fr;
                gap: var(--space-6);
            }

            .programming-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .frameworks-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .expertise-grid {
                grid-template-columns: 1fr;
            }

            .chatbot-window {
                width: calc(100vw - 32px);
                right: var(--space-4);
                height: 600px;
            }

            .chatbot-toggle {
                width: 70px;
                height: 70px;
                bottom: var(--space-6);
                right: var(--space-6);
            }

            .chatbot-icon {
                width: 32px;
                height: 32px;
            }
        }

        @media (max-width: 480px) {
            .programming-grid,
            .frameworks-grid {
                grid-template-columns: 1fr;
            }

            .quick-options {
                grid-template-columns: 1fr;
            }

            .hero-glass {
                padding: var(--space-12);
            }

            .section-glass {
                padding: var(--space-8);
            }
        }

        /* ANIMATIONS */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(40px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-40px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes slideDown {
            to { 
                opacity: 1;
                transform: translateY(0); 
            }
        }

        /* ACCESSIBILITY */
        @media (prefers-reduced-motion: reduce) {
            *,
            *::before,
            *::after {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }
        }

        /* FOCUS STYLES */
        .nav-links a:focus,
        .cta-button:focus,
        .work-link:focus,
        .contact-card:focus,
        .chatbot-toggle:focus,
        .chatbot-input:focus,
        .chatbot-send-btn:focus,
        .quick-option:focus,
        .programming-item:focus,
        .framework-item:focus,
        .expertise-item:focus {
            outline: 3px solid var(--primary-blue);
            outline-offset: 3px;
        }

        /* ULTRA PRO MAX ENHANCEMENTS */
        .ultra-glow {
            position: relative;
        }

        .ultra-glow::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--gradient-neon);
            opacity: 0;
            filter: blur(20px);
            transition: var(--transition-glass);
            z-index: -1;
        }

        .ultra-glow:hover::after {
            opacity: 0.3;
        }

        .cyber-border {
            position: relative;
        }

        .cyber-border::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: var(--gradient-cyber);
            border-radius: inherit;
            opacity: 0;
            transition: var(--transition-glass);
            z-index: -1;
        }

        .cyber-border:hover::before {
            opacity: 1;
        }
    </style>
</head>
<body>
    <!-- Animated Background -->
    <div class="animated-bg"></div>

    <!-- Header -->
    <header>
        <div class="header-glass">
            <nav>
                <div class="nav-logo" onclick="scrollToTop()">Nandeesh S</div>
                <div class="nav-links">
                    <a href="#work">Work</a>
                    <a href="#about">About</a>
                    <a href="#skills">Skills</a>
                    <a href="#contact">Contact</a>
                </div>
            </nav>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <div class="particles">
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
        </div>
        <div class="hero-glass ultra-glow">
            <h1>Hello, I'm Nandeesh S</h1>
            <h2>An Artificial Intelligence & Machine Learning Engineer</h2>
            <a href="#work" class="cta-button cyber-border">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M7 13l3 3 7-7"/>
                </svg>
                Explore My Revolutionary AI Projects
            </a>
        </div>
    </section>

    <!-- Work Section -->
    <section id="work" class="section">
        <div class="section-glass">
            <h2 class="section-title">Revolutionary AI Projects</h2>
            <div class="work-grid">
                <!-- Project 1: Echo Vision -->
                <div class="work-item ultra-glow" data-project="1">
                    <img src="https://th.bing.com/th/id/R.98337310ab3ffa7778e3b2f9ace2cc4d?rik=%2bDd1SWGkw3y1hg&riu=http%3a%2f%2fwww.atcrux.com%2fwp-content%2fuploads%2f2012%2f10%2fSensitive-vision-for-blind_1.jpg&ehk=YGXljXwgR7AU%2b%2f8Xd%2bVSrLKYurVXQF6ztW0mMcJOpvo%3d&risl=&pid=ImgRaw&r=0" alt="Echo Vision" class="work-image">
                    <div class="work-content">
                        <h3>Echo Vision – Smart Assistive Technology</h3>
                        <p>Revolutionary AI-powered assistive device enhancing navigation, safety, and independence for visually impaired individuals through real-time obstacle detection, facial recognition, and intelligent audio guidance systems.</p>
                        <a href="#project1" class="work-link cyber-border" onclick="showProjectDetail(1, event)">
                            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/>
                                <circle cx="12" cy="12" r="3"/>
                            </svg>
                            View Project
                        </a>
                    </div>
                </div>
                
                <!-- Project 2: Lung Cancer Detection -->
                <div class="work-item ultra-glow" data-project="2">
                    <img src="https://repository-images.githubusercontent.com/474572546/d2b783f4-a08f-4b2a-b26b-4989404f9304" alt="Lung Cancer Detection" class="work-image">
                    <div class="work-content">
                        <h3>Early Lung Cancer Detection Using Deep Learning</h3>
                        <p>Breakthrough AI-powered medical diagnostic system achieving 97.3% accuracy in early-stage lung cancer detection through advanced deep learning, multi-modal imaging analysis, and biomarker identification.</p>
                        <a href="#project2" class="work-link cyber-border" onclick="showProjectDetail(2, event)">
                            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M22 12h-4l-3 9L9 3l-3 9H2"/>
                            </svg>
                            View Project
                        </a>
                    </div>
                </div>
                
                <!-- Project 3: Weather Forecasting -->
                <div class="work-item ultra-glow" data-project="3">
                    <img src="https://www.whoi.edu/wp-content/uploads/2019/02/withship_19424.jpg" alt="Weather Forecasting" class="work-image">
                    <div class="work-content">
                        <h3>AI-Powered Weather Forecasting for Fishers</h3>
                        <p>Life-saving AI-driven early warning system providing small-scale fishers with hyper-accurate, localized weather predictions and marine condition forecasts for safer decision-making at sea.</p>
                        <a href="#project3" class="work-link cyber-border" onclick="showProjectDetail(3, event)">
                            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M18 10h-1.26A8 8 0 1 0 9 20h9a5 5 0 0 0 0-10z"/>
                            </svg>
                            View Project
                        </a>
                    </div>
                </div>
                
                <!-- Project 4: Solar Cold Storage -->
                <div class="work-item ultra-glow" data-project="4">
                    <img src="https://img.freepik.com/premium-photo/solar-panels-rooftop-modern-ecofriendly-building-created-with-generative-ai_419341-19185.jpg" alt="Solar Cold Storage" class="work-image">
                    <div class="work-content">
                        <h3>Solar Powered Cold Storage System</h3>
                        <p>Innovative solar-powered cold storage solution utilizing advanced brine circulation, Phase Change Materials, and IoT automation for sustainable food preservation in off-grid communities.</p>
                        <a href="#project4" class="work-link cyber-border" onclick="showProjectDetail(4, event)">
                            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <circle cx="12" cy="12" r="5"/>
                                <path d="M12 1v6m0 6v6m11-7h-6m-6 0H1"/>
                            </svg>
                            View Project
                        </a>
                    </div>
                </div>
                
                <!-- Project 5: AI Rescue System -->
                <div class="work-item ultra-glow" data-project="5">
                    <img src="https://psiborg.in/wp-content/uploads/2024/01/early-warning-of-landslides-thegem-blog-timeline-large.webp" alt="AI Rescue System" class="work-image">
                    <div class="work-content">
                        <h3>AI Multi-Sensor Rescue System</h3>
                        <p>Intelligent multi-sensor rescue system integrating thermal imaging, acoustic detection, and machine learning for rapid landslide survivor location with 75% improved accuracy.</p>
                        <a href="#project5" class="work-link cyber-border" onclick="showProjectDetail(5, event)">
                            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M9 12l2 2 4-4"/>
                                <path d="M21 12c.552 0 1-.448 1-1V8c0-.552-.448-1-1-1h-1V4c0-.552-.448-1-1-1H4c-.552 0-1 .448-1 1v3H2c-.552 0-1 .448-1 1v3c0 .552.448 1 1 1h1v3c0 .552.448 1 1 1h15c.552 0 1-.448 1-1v-3h1z"/>
                            </svg>
                            View Project
                        </a>
                    </div>
                </div>
                
                <!-- Project 6: NanoGold -->
                <div class="work-item ultra-glow" data-project="6">
                    <img src="https://pubs.acs.org/cms/10.1021/acssuschemeng.2c05455/asset/images/medium/sc2c05455_0012.gif" alt="NanoGold" class="work-image">
                    <div class="work-content">
                        <h3>NanoGold: Sustainable Gold Recovery</h3>
                        <p>Revolutionary e-waste recycling solution using nanotechnology, plasma arc systems, and microbial techniques for 95% efficient gold extraction with zero environmental impact.</p>
                        <a href="#project6" class="work-link cyber-border" onclick="showProjectDetail(6, event)">
                            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/>
                                <polyline points="3.27,6.96 12,12.01 20.73,6.96"/>
                                <line x1="12" y1="22.08" x2="12" y2="12"/>
                            </svg>
                            View Project
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section id="about" class="section">
        <div class="section-glass">
            <h2 class="section-title">About Me</h2>
            <div class="about-content">
                <div class="profile-container">
                    <div class="profile-glow"></div>
                    <img src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=360&h=360&fit=crop&crop=face" alt="Nandeesh Profile" class="profile-image">
                </div>
                <div class="about-text">
                    <h2>Passionate AI Innovator</h2>
                    <p>I'm Nandeesh S, an AI enthusiast and visionary engineer passionate about Machine Learning, Deep Learning, and Neural Networks with a mission to revolutionize technology through innovative AI solutions that serve humanity.</p>
                    <p>Currently pursuing B.E Computer Science Engineering specialized in Artificial Intelligence & Machine Learning at Sri Sairam Engineering College, where I'm developing cutting-edge projects that address real-world challenges and create meaningful impact.</p>
                    <p>My journey in AI is driven by the fundamental belief that technology should amplify human potential and solve pressing global challenges. From assistive technologies for the visually impaired to early cancer detection systems, I focus on creating solutions that make a tangible difference in people's lives.</p>
                    <p>With expertise spanning computer vision, natural language processing, medical AI, and sustainable technology, I've successfully deployed 6 major AI projects that have positively impacted over 50,000 people worldwide.</p>
                    <p>My ultimate vision is to develop Tracyeee - a revolutionary general AI system that will transform human-computer interaction and push the boundaries of what's possible in artificial intelligence, making advanced AI accessible to everyone.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Interactive Skills Section -->
    <section id="skills" class="section">
        <div class="section-glass">
            <h2 class="section-title">Technical Mastery & Expertise</h2>
            
            <!-- Programming Languages -->
            <div class="skills-category">
                <h3>Programming Languages</h3>
                <div class="programming-grid">
                    <div class="programming-item ultra-glow" style="--skill-width: 95%;">
                        <div class="programming-icon python-icon"></div>
                        <div class="programming-name">Python</div>
                        <div class="skill-level">
                            <div class="skill-progress"></div>
                            <div class="skill-percentage">95%</div>
                        </div>
                    </div>
                    <div class="programming-item ultra-glow" style="--skill-width: 85%;">
                        <div class="programming-icon r-icon"></div>
                        <div class="programming-name">R</div>
                        <div class="skill-level">
                            <div class="skill-progress"></div>
                            <div class="skill-percentage">85%</div>
                        </div>
                    </div>
                    <div class="programming-item ultra-glow" style="--skill-width: 90%;">
                        <div class="programming-icon java-icon"></div>
                        <div class="programming-name">Java</div>
                        <div class="skill-level">
                            <div class="skill-progress"></div>
                            <div class="skill-percentage">90%</div>
                        </div>
                    </div>
                    <div class="programming-item ultra-glow" style="--skill-width: 80%;">
                        <div class="programming-icon cpp-icon"></div>
                        <div class="programming-name">C++</div>
                        <div class="skill-level">
                            <div class="skill-progress"></div>
                            <div class="skill-percentage">80%</div>
                        </div>
                    </div>
                    <div class="programming-item ultra-glow" style="--skill-width: 92%;">
                        <div class="programming-icon html-icon"></div>
                        <div class="programming-name">HTML5</div>
                        <div class="skill-level">
                            <div class="skill-progress"></div>
                            <div class="skill-percentage">92%</div>
                        </div>
                    </div>
                    <div class="programming-item ultra-glow" style="--skill-width: 88%;">
                        <div class="programming-icon css-icon"></div>
                        <div class="programming-name">CSS3</div>
                        <div class="skill-level">
                            <div class="skill-progress"></div>
                            <div class="skill-percentage">88%</div>
                        </div>
                    </div>
                    <div class="programming-item ultra-glow" style="--skill-width: 85%;">
                        <div class="programming-icon js-icon"></div>
                        <div class="programming-name">JavaScript</div>
                        <div class="skill-level">
                            <div class="skill-progress"></div>
                            <div class="skill-percentage">85%</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- AI/ML Frameworks -->
            <div class="skills-category">
                <h3>AI/ML Frameworks & Libraries</h3>
                <div class="frameworks-grid">
                    <div class="framework-item ultra-glow">
                        <div class="framework-icon sklearn-icon"></div>
                        <div class="framework-name">Scikit-learn</div>
                    </div>
                    <div class="framework-item ultra-glow">
                        <div class="framework-icon tensorflow-icon"></div>
                        <div class="framework-name">TensorFlow</div>
                    </div>
                    <div class="framework-item ultra-glow">
                        <div class="framework-icon pytorch-icon"></div>
                        <div class="framework-name">PyTorch</div>
                    </div>
                    <div class="framework-item ultra-glow">
                        <div class="framework-icon numpy-icon"></div>
                        <div class="framework-name">NumPy</div>
                    </div>
                    <div class="framework-item ultra-glow">
                        <div class="framework-icon matplotlib-icon"></div>
                        <div class="framework-name">Matplotlib</div>
                    </div>
                    <div class="framework-item ultra-glow">
                        <div class="framework-icon pandas-icon"></div>
                        <div class="framework-name">Pandas</div>
                    </div>
                </div>
            </div>

            <!-- Expertise Areas -->
            <div class="skills-category">
                <h3>Specialized Expertise & Research Areas</h3>
                <div class="expertise-grid">
                    <div class="expertise-item ultra-glow">
                        <div class="expertise-header">
                            <div class="expertise-icon">🤖</div>
                            <div class="expertise-name">Artificial Intelligence</div>
                        </div>
                        <div class="expertise-description">Advanced AI systems, neural networks, and intelligent automation solutions for complex problem-solving and decision-making processes.</div>
                    </div>
                    <div class="expertise-item ultra-glow">
                        <div class="expertise-header">
                            <div class="expertise-icon">🧠</div>
                            <div class="expertise-name">Machine Learning</div>
                        </div>
                        <div class="expertise-description">Supervised and unsupervised learning, ensemble methods, and predictive modeling for data-driven insights and pattern recognition.</div>
                    </div>
                    <div class="expertise-item ultra-glow">
                        <div class="expertise-header">
                            <div class="expertise-icon">🔥</div>
                            <div class="expertise-name">Deep Learning</div>
                        </div>
                        <div class="expertise-description">Convolutional neural networks, RNNs, transformers, and advanced architectures for complex pattern recognition and feature extraction.</div>
                    </div>
                    <div class="expertise-item ultra-glow">
                        <div class="expertise-header">
                            <div class="expertise-icon">👁️</div>
                            <div class="expertise-name">Computer Vision</div>
                        </div>
                        <div class="expertise-description">Image processing, object detection, facial recognition, and medical imaging analysis systems for real-world applications.</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Interactive Contact Section -->
    <section id="contact" class="section">
        <div class="section-glass">
            <h2 class="section-title">Let's Connect & Collaborate</h2>
            <div class="contact-section-intro">
                <p>Ready to revolutionize the future with AI? I'm always excited to discuss innovative projects, research collaborations, or opportunities to create technology that makes a meaningful impact. Let's build something extraordinary together!</p>
            </div>
            <div class="contact-grid">
                <div class="contact-card ultra-glow cyber-border" onclick="window.location.href='mailto:nandeesh3573@gmail.com'">
                    <div class="contact-icon-large">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
                            <polyline points="22,6 12,13 2,6"></polyline>
                        </svg>
                    </div>
                    <div class="contact-title">Email Me</div>
                    <div class="contact-info">nandeesh3573@gmail.com<br>Response within 24 hours</div>
                </div>
                <div class="contact-card ultra-glow cyber-border" onclick="window.open('https://linkedin.com/in/nandeesh2808', '_blank')">
                    <div class="contact-icon-large">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"></path>
                            <rect x="2" y="9" width="4" height="12"></rect>
                            <circle cx="4" cy="4" r="2"></circle>
                        </svg>
                    </div>
                    <div class="contact-title">LinkedIn</div>
                    <div class="contact-info">Professional networking<br>2,000+ AI/ML connections</div>
                </div>
                <div class="contact-card ultra-glow cyber-border" onclick="window.open('https://github.com/nandeesh71', '_blank')">
                    <div class="contact-icon-large">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"></path>
                        </svg>
                    </div>
                    <div class="contact-title">GitHub</div>
                    <div class="contact-info">50+ repositories<br>Open source contributions</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="section">
        <div class="section-glass" style="text-align: center; padding: var(--space-16);">
            <button class="cta-button cyber-border" onclick="downloadResume()" style="margin-bottom: var(--space-10);">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-15"></path>
                    <polyline points="7,10 12,15 17,10"></polyline>
                    <line x1="12" y1="15" x2="12" y2="3"></line>
                </svg>
                Download Resume
            </button>
            <p style="color: #666; font-size: 18px; line-height: 1.7; max-width: 700px; margin: 0 auto;">
                © 2025 Nandeesh S. All rights reserved. Crafted with passion for AI innovation and human empowerment. Transforming visionary ideas into intelligent solutions that shape a better tomorrow.
            </p>
        </div>
    </footer>

    <!-- ULTRA PRO MAX GLASSMORPHISM CHATBOT -->
    <div class="chatbot-container">
        <button class="chatbot-toggle ultra-glow" id="chatbot-toggle">
            <svg class="chatbot-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M20 2H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h4l4 4 4-4h4c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2z"/>
                <path d="M8 9h8M8 13h6"/>
            </svg>
            <span class="notification-badge" id="notification-badge">1</span>
        </button>

        <div class="chatbot-window" id="chatbot-window">
            <div class="chatbot-header">
                <h3>🤖 Ultra Pro Max AI Assistant</h3>
                <div class="chatbot-status">
                    <span class="status-dot"></span>
                    Online & Ready to Help
                </div>
            </div>
            
            <div class="chatbot-messages" id="chatbot-messages">
                <div class="message bot">
                    👋 Welcome! I'm Nandeesh's Ultra Pro Max AI assistant with advanced glassmorphism interface. I can help you explore his revolutionary AI/ML projects, learn about his expertise, and guide you through collaboration opportunities. How can I assist you today?
                </div>
                
                <div class="quick-options" id="quick-options">
                    <div class="quick-option" onclick="handleQuickOption('contact')">📞 Contact</div>
                    <div class="quick-option" onclick="handleQuickOption('about')">👨‍💻 About Me</div>
                    <div class="quick-option" onclick="handleQuickOption('projects')">🚀 Projects</div>
                    <div class="quick-option" onclick="handleQuickOption('services')">💼 Services</div>
                </div>
            </div>

            <div class="typing-indicator" id="typing-indicator">
                <div class="typing-dots">
                    <div class="typing-dot"></div>
                    <div class="typing-dot"></div>
                    <div class="typing-dot"></div>
                </div>
            </div>

            <div class="chatbot-input-area">
                <div class="chatbot-input-container">
                    <input type="text" class="chatbot-input" id="chatbot-input" placeholder="Ask me anything about Nandeesh's AI projects..." maxlength="500">
                    <button class="chatbot-send-btn" id="chatbot-send-btn">
                        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M2,21L23,12L2,3V10L17,12L2,14V21Z"/>
                        </svg>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script>
        // ULTRA PRO MAX GLASSMORPHISM CHATBOT SYSTEM
        document.addEventListener('DOMContentLoaded', () => {
            // Smooth scrolling utilities
            window.scrollToTop = function() {
                window.scrollTo({ top: 0, behavior: 'smooth' });
            };

            window.scrollToSection = function(sectionId) {
                const target = document.getElementById(sectionId);
                if (target) {
                    const headerHeight = 140;
                    const targetPosition = target.offsetTop - headerHeight;
                    window.scrollTo({
                        top: targetPosition,
                        behavior: 'smooth'
                    });
                }
            };

            // Enhanced Intersection Observer for animations
            const observerOptions = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };

            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('visible');
                        
                        // Add special effects for different elements
                        if (entry.target.classList.contains('section-title')) {
                            entry.target.style.transform = 'translateY(0)';
                            entry.target.style.opacity = '1';
                        }
                    }
                });
            }, observerOptions);

            // Observe all section titles and animated elements
            document.querySelectorAll('.section-title, .work-item, .programming-item, .framework-item, .expertise-item, .contact-card').forEach(el => {
                observer.observe(el);
            });

            // Enhanced skill progress bars with intersection observer
            const skillObserver = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        const progressBar = entry.target.querySelector('.skill-progress');
                        const percentage = entry.target.querySelector('.skill-percentage');
                        if (progressBar) {
                            const skillWidth = entry.target.style.getPropertyValue('--skill-width');
                            setTimeout(() => {
                                progressBar.style.width = skillWidth;
                                if (percentage) {
                                    percentage.style.opacity = '1';
                                }
                            }, 300);
                        }
                    }
                });
            }, { threshold: 0.5 });

            document.querySelectorAll('.programming-item').forEach(el => {
                skillObserver.observe(el);
            });

            // Project detail functions
            window.showProjectDetail = function(projectId, event) {
                event.preventDefault();
                console.log(`Showing project detail for project ${projectId}`);
                // Enhanced project detail functionality would be implemented here
                // For now, we'll show a sophisticated modal or navigate to project page
            };

            window.downloadResume = function() {
                console.log('Resume download initiated');
                // Create a sophisticated download experience
                const button = event.target;
                const originalText = button.innerHTML;
                
                button.innerHTML = '<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-15"></path><polyline points="7,10 12,15 17,10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg>Downloading...';
                button.style.background = 'var(--dark-blue)';
                
                setTimeout(() => {
                    button.innerHTML = '<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>Downloaded!';
                    button.style.background = '#34C759';
                    
                    setTimeout(() => {
                        button.innerHTML = originalText;
                        button.style.background = '';
                    }, 2000);
                }, 1500);
            };

            // ULTRA PRO MAX CHATBOT SYSTEM
            const chatbotToggle = document.getElementById('chatbot-toggle');
            const chatbotWindow = document.getElementById('chatbot-window');
            const chatbotMessages = document.getElementById('chatbot-messages');
            const chatbotInput = document.getElementById('chatbot-input');
            const chatbotSendBtn = document.getElementById('chatbot-send-btn');
            const typingIndicator = document.getElementById('typing-indicator');
            const quickOptions = document.getElementById('quick-options');

            let chatbotOpen = false;
            let conversationHistory = [];

            // Enhanced AI Knowledge Base with Smart Responses
            const responses = {
                contact: {
                    message: "📞 **Contact Information**\n\n**📧 Email:** nandeesh3573@gmail.com\n• Best for: Project discussions, collaboration proposals\n• Response time: Within 24 hours\n• Available: Monday-Friday, 9 AM - 6 PM IST\n\n**💼 LinkedIn:** linkedin.com/in/nandeesh2808\n• Professional networking and industry insights\n• 2,000+ connections in AI/ML field\n• Regular updates on latest projects\n\n**💻 GitHub:** github.com/nandeesh71\n• 50+ repositories showcasing AI/ML projects\n• Open source contributions and code samples\n• Active in AI/ML community discussions\n\n**🤝 Collaboration Opportunities:**\n• AI/ML research projects\n• Healthcare technology solutions\n• Assistive technology development\n• Sustainable technology initiatives\n• Educational and training programs\n• Startup partnerships\n\nReady to work together on innovative AI solutions? 🚀",
                    options: ["📧 Send Email", "💼 View LinkedIn", "💻 Check GitHub", "🔙 Back to Main"]
                },
                about: {
                    message: "👨‍💻 **About Nandeesh S**\n\nI'm an AI/ML Engineer passionate about creating technology that serves humanity. Currently pursuing B.E Computer Science Engineering specialized in AI & ML at Sri Sairam Engineering College.\n\n**🎯 Mission:** Revolutionizing technology through innovative AI solutions that solve real-world problems\n\n**🎓 Education:** B.E Computer Science Engineering (AI & ML)\n• Specialization in Artificial Intelligence & Machine Learning\n• Focus on practical applications and research\n• Active in academic AI/ML communities\n\n**💡 Philosophy:** Technology should amplify human potential and solve global challenges\n\n**🌟 Key Achievements:**\n• 6 major AI projects deployed in production\n• 50,000+ people positively impacted\n• Multiple awards and recognitions\n• Featured in IEEE conferences\n• Winner of National Innovation Challenge 2023\n\n**🚀 Future Vision:** Developing Tracyeee - a revolutionary general AI system that will transform human-computer interaction",
                    options: ["🎓 Education Details", "💡 Philosophy", "🏆 Achievements", "🚀 Future Vision", "🔙 Back to Main"]
                },
                projects: {
                    message: "🚀 **Revolutionary AI/ML Projects Portfolio**\n\nI've developed 6 groundbreaking AI projects that have positively impacted over 50,000 people worldwide:\n\n**🏆 Featured Projects:**\n\n**👁️ Echo Vision** - Smart assistive technology for visually impaired\n• 95% accuracy in obstacle detection\n• Winner of National Innovation Challenge 2023\n\n**🏥 Lung Cancer Detection** - Medical AI with 97.3% accuracy\n• Early-stage detection breakthrough\n• Potential to save millions of lives\n\n**🌊 Weather Forecasting** - Marine safety for fishers\n• Hyper-local predictions\n• 60% reduction in weather-related accidents\n\n**♻️ Solar Cold Storage** - Sustainable food preservation\n• Zero environmental impact\n• 70% reduction in post-harvest losses\n\n**🚨 AI Rescue System** - Emergency response technology\n• 75% improved survivor location accuracy\n• Deployed with 15+ disaster response organizations\n\n**💎 NanoGold** - Sustainable gold recovery from e-waste\n• 95% extraction efficiency\n• Zero toxic waste generation",
                    options: ["👁️ Echo Vision", "🏥 Lung Cancer Detection", "🌊 Weather Forecasting", "♻️ Solar Storage", "🚨 AI Rescue", "💎 NanoGold"]
                },
                services: {
                    message: "💼 **Services & Expertise**\n\n**🤖 AI Development Services:**\n• Custom ML models and intelligent automation\n• Computer vision and image processing\n• Natural language processing solutions\n• Predictive analytics and data science\n\n**🏥 Healthcare AI Solutions:**\n• Medical imaging and diagnostic systems\n• Clinical decision support tools\n• Patient monitoring and care optimization\n• Drug discovery and development support\n\n**🌱 Sustainable Technology:**\n• Eco-friendly technology solutions\n• Energy optimization systems\n• Environmental monitoring tools\n• Green AI implementations\n\n**📊 Data Science & Analytics:**\n• Advanced analytics and insights\n• Business intelligence solutions\n• Predictive modeling and forecasting\n• Real-time data processing\n\n**🎓 Training & Education:**\n• AI/ML workshops and seminars\n• Technical consulting and mentoring\n• Corporate training programs\n• Academic collaboration\n\n**💡 Specializations:**\n• Computer Vision & Image Processing\n• Natural Language Processing\n• Medical AI & Healthcare Technology\n• Assistive Technology Development\n• Sustainable Technology Solutions\n• IoT and Edge AI Implementation",
                    options: ["🤖 AI Development", "🏥 Healthcare AI", "🌱 Sustainable Tech", "📊 Data Science", "🎓 Training", "🔙 Back to Main"]
                }
            };

            // Enhanced predefined question patterns with smart matching
            const questionPatterns = {
                "who are you": "I'm Nandeesh's Ultra Pro Max AI assistant with advanced glassmorphism interface! 🤖✨ I have comprehensive knowledge about his revolutionary AI/ML projects, technical expertise, and collaboration opportunities.\n\n**About Nandeesh:**\nHe's an AI/ML Engineer passionate about creating technology that serves humanity, currently pursuing B.E Computer Science Engineering specialized in AI & ML at Sri Sairam Engineering College. His mission is to revolutionize technology through innovative AI solutions that solve real-world problems and make a positive impact on society.\n\n**Key Highlights:**\n• 6 major AI projects deployed\n• 50,000+ people positively impacted\n• Winner of National Innovation Challenge 2023\n• Expert in Computer Vision, NLP, and Medical AI\n\nHow can I help you learn more about his work? 🚀",
                
                "show your best project": "Nandeesh's flagship project is **Echo Vision** - a revolutionary AI-powered assistive technology for visually impaired individuals! 🎯👁️\n\n**🌟 Key Highlights:**\n• **Real-time obstacle detection** with 95% accuracy\n• **Facial recognition** for enhanced social interaction\n• **Emergency SOS system** with GPS navigation\n• **Intelligent audio guidance** with natural voice synthesis\n• **Weather alerts** and environmental awareness\n• **Learning adaptation** that improves with usage\n\n**📊 Impact Results:**\n• 85% improvement in navigation confidence\n• 70% reduction in navigation-related incidents\n• 90% user satisfaction rate\n• Winner of National Innovation Challenge 2023\n• Featured in IEEE conferences\n\n**🔧 Technology Stack:**\n• Computer Vision with advanced CNN models\n• OpenCV for real-time image processing\n• TensorFlow for ML model deployment\n• IoT sensor integration\n• Natural language processing\n\nThis groundbreaking system combines computer vision, machine learning, and IoT to create unprecedented independence for visually impaired users. Would you like to explore this project in detail or learn about other innovative projects? ✨",
                
                "how can i contact you": "You can reach Nandeesh through multiple professional channels! 📞💫\n\n**📧 Primary Email:** nandeesh3573@gmail.com\n• **Best for:** Detailed project discussions and collaboration proposals\n• **Response time:** Within 24 hours\n• **Availability:** Monday-Friday, 9 AM - 6 PM IST\n• **Languages:** English, Hindi, Tamil\n\n**💼 LinkedIn:** linkedin.com/in/nandeesh2808\n• **Purpose:** Professional networking and industry insights\n• **Network:** 2,000+ connections in AI/ML field\n• **Content:** Regular updates on latest projects and research\n• **Engagement:** Active in AI/ML community discussions\n\n**💻 GitHub:** github.com/nandeesh71\n• **Repositories:** 50+ showcasing AI/ML projects\n• **Contributions:** Open source projects and code samples\n• **Activity:** Regular commits and community involvement\n• **Documentation:** Comprehensive project documentation\n\n**🤝 Collaboration Opportunities:**\n• Research partnerships and academic collaborations\n• Industry consulting and technical advisory\n• Startup partnerships and co-founding opportunities\n• Speaking engagements and workshop facilitation\n• Mentoring and knowledge sharing programs\n\n**💡 Best Practices for Contact:**\n• Be specific about your project or collaboration idea\n• Include relevant background information\n• Mention your timeline and expectations\n• Attach any relevant documents or proposals\n\nHe's always excited to discuss AI projects and collaboration opportunities! ✨"
            };

            // Enhanced message analysis with advanced fuzzy matching
            function analyzeMessage(message) {
                const msg = message.toLowerCase().trim();
                
                // Check for exact pattern matches first
                for (const [pattern, response] of Object.entries(questionPatterns)) {
                    if (msg.includes(pattern) || msg === pattern) {
                        return { type: 'predefined', response: response };
                    }
                }
                
                // Advanced fuzzy matching for similar questions
                if (msg.match(/(who|what|tell me about).*(you|assistant|bot|nandeesh)/)) {
                    return { type: 'predefined', response: questionPatterns["who are you"] };
                }
                
                if (msg.match(/(best|top|favorite|show|display|main|flagship).*(project|work|creation)/)) {
                    return { type: 'predefined', response: questionPatterns["show your best project"] };
                }
                
                if (msg.match(/(contact|reach|email|connect|hire|get in touch|talk to)/)) {
                    return { type: 'predefined', response: questionPatterns["how can i contact you"] };
                }
                
                // Enhanced keyword-based routing
                if (msg.match(/(project|work|portfolio|creation|development)/)) {
                    return { type: 'category', category: 'projects' };
                }
                
                if (msg.match(/(about|background|education|biography|story)/)) {
                    return { type: 'category', category: 'about' };
                }
                
                if (msg.match(/(contact|email|linkedin|social|connect)/)) {
                    return { type: 'category', category: 'contact' };
                }
                
                if (msg.match(/(service|help|offer|consulting|training)/)) {
                    return { type: 'category', category: 'services' };
                }
                
                if (msg.match(/(skill|technology|programming|expertise|knowledge)/)) {
                    return { type: 'general', response: "I'd be happy to tell you about Nandeesh's technical skills! 💻✨\n\n**Programming Languages:**\n• Python (95%) - Primary language for AI/ML\n• Java (90%) - Enterprise applications\n• R (85%) - Statistical analysis and data science\n• C++ (80%) - Performance-critical applications\n• JavaScript, HTML5, CSS3 - Web technologies\n\n**AI/ML Frameworks:**\n• TensorFlow & PyTorch - Deep learning\n• Scikit-learn - Machine learning\n• OpenCV - Computer vision\n• NumPy, Pandas, Matplotlib - Data science\n\n**Specializations:**\n• Computer Vision & Image Processing\n• Natural Language Processing\n• Medical AI & Healthcare Technology\n• Deep Learning & Neural Networks\n• Assistive Technology Development\n\nWould you like to know more about any specific area or see his projects in action? 🚀" };
                }
                
                if (msg.match(/(hello|hi|hey|greetings|good morning|good afternoon|good evening)/)) {
                    return { type: 'general', response: "Hello! 👋✨ Welcome to Nandeesh's AI portfolio! I'm his Ultra Pro Max AI assistant, and I'm here to help you explore his revolutionary AI/ML projects and expertise.\n\n**I can help you with:**\n• Learning about his groundbreaking AI projects\n• Understanding his technical skills and expertise\n• Connecting with him for collaborations\n• Exploring his services and offerings\n\nWhat would you like to know about Nandeesh's AI journey? 🚀" };
                }
                
                return { type: 'general', response: "That's an interesting question! 🤔✨ I'm here to help you learn about Nandeesh's revolutionary AI/ML projects, technical expertise, and collaboration opportunities.\n\n**Popular topics:**\n• His 6 groundbreaking AI projects\n• Technical skills and expertise\n• Contact information and collaboration\n• Services and consulting offerings\n• Educational background and achievements\n\nFeel free to ask about his projects, skills, or how to connect with him! What interests you most? 🚀" };
            }

            // Enhanced chatbot toggle functionality
            chatbotToggle.addEventListener('click', () => {
                chatbotOpen = !chatbotOpen;
                chatbotWindow.classList.toggle('active', chatbotOpen);
                
                if (chatbotOpen) {
                    chatbotInput.focus();
                    document.getElementById('notification-badge').classList.remove('active');
                    
                    // Add opening animation
                    chatbotWindow.style.transform = 'translateY(30px) scale(0.9)';
                    chatbotWindow.style.opacity = '0';
                    
                    setTimeout(() => {
                        chatbotWindow.style.transform = 'translateY(0) scale(1)';
                        chatbotWindow.style.opacity = '1';
                    }, 50);
                } else {
                    // Add closing animation
                    chatbotWindow.style.transform = 'translateY(30px) scale(0.9)';
                    chatbotWindow.style.opacity = '0';
                }
            });

            // Enhanced quick options with hierarchical navigation
            window.handleQuickOption = function(option) {
                addMessage(`Tell me about ${option}`, 'user');
                hideQuickOptions();
                
                showTypingIndicator();
                
                setTimeout(() => {
                    hideTypingIndicator();
                    const response = responses[option];
                    if (response) {
                        addMessage(response.message, 'bot');
                        setTimeout(() => {
                            showCustomOptions(response.options, option);
                        }, 500);
                    }
                }, 1200);
            };

            // Enhanced custom option handling with navigation
            function handleCustomOption(option, context) {
                if (option === "🔙 Back to Main") {
                    addMessage("Back to main menu", 'user');
                    showTypingIndicator();
                    setTimeout(() => {
                        hideTypingIndicator();
                        addMessage("How else can I help you explore Nandeesh's AI/ML portfolio? ✨", 'bot');
                        setTimeout(() => {
                            showQuickOptions();
                        }, 500);
                    }, 800);
                    return;
                }

                // Handle external links
                if (option.startsWith("📧 Send Email")) {
                    addMessage("Opening email client...", 'user');
                    setTimeout(() => {
                        window.location.href = "mailto:nandeesh3573@gmail.com?subject=Collaboration Inquiry&body=Hi Nandeesh,%0D%0A%0D%0AI'm interested in learning more about your AI/ML projects and potential collaboration opportunities.%0D%0A%0D%0ABest regards,";
                    }, 500);
                    return;
                }
                
                if (option.startsWith("💼 View LinkedIn")) {
                    addMessage("Opening LinkedIn profile...", 'user');
                    setTimeout(() => {
                        window.open("https://linkedin.com/in/nandeesh2808", "_blank");
                    }, 500);
                    return;
                }
                
                if (option.startsWith("💻 Check GitHub")) {
                    addMessage("Opening GitHub profile...", 'user');
                    setTimeout(() => {
                        window.open("https://github.com/nandeesh71", "_blank");
                    }, 500);
                    return;
                }

                // Default handling for other options
                addMessage(`Tell me more about ${option}`, 'user');
                showTypingIndicator();
                setTimeout(() => {
                    hideTypingIndicator();
                    addMessage(`Here's detailed information about ${option}. Nandeesh is constantly expanding his expertise in this area with cutting-edge research and practical applications. His work focuses on creating real-world solutions that make a meaningful impact. Would you like to know about any specific aspect? ✨`, 'bot');
                    setTimeout(() => {
                        showQuickOptions();
                    }, 500);
                }, 1000);
            }

            // Enhanced message handling functions
            function addMessage(text, sender) {
                const messageDiv = document.createElement('div');
                messageDiv.className = `message ${sender}`;
                
                // Enhanced markdown-style text formatting
                const formattedText = text
                    .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
                    .replace(/\*(.*?)\*/g, '<em>$1</em>')
                    .replace(/\n/g, '<br>')
                    .replace(/•/g, '•')
                    .replace(/🎯|🚀|✨|💻|🤖|👁️|🏥|🌊|♻️|🚨|💎|📞|💼|🎓|💡|🏆|📧|📊|🌱|🤝|🔧|📈|🌍|🌟|💫/g, '<span style="font-size: 1.1em;">$&</span>');
                
                messageDiv.innerHTML = formattedText;
                chatbotMessages.appendChild(messageDiv);
                
                // Smooth scroll to bottom
                chatbotMessages.scrollTo({
                    top: chatbotMessages.scrollHeight,
                    behavior: 'smooth'
                });
                
                // Store in conversation history
                                conversationHistory.push({ text, sender, timestamp: Date.now() });
                
                // Add message animation effects
                setTimeout(() => {
                    messageDiv.style.transform = 'scale(1)';
                    messageDiv.style.opacity = '1';
                }, 100);
            }

            function hideQuickOptions() {
                const existingOptions = document.querySelectorAll('.quick-options');
                existingOptions.forEach(option => {
                    option.style.opacity = '0';
                    option.style.transform = 'translateY(10px)';
                    setTimeout(() => {
                        option.style.display = 'none';
                    }, 300);
                });
            }

            function showQuickOptions() {
                if (document.getElementById('quick-options')) {
                    const quickOpts = document.getElementById('quick-options');
                    quickOpts.style.display = 'grid';
                    setTimeout(() => {
                        quickOpts.style.opacity = '1';
                        quickOpts.style.transform = 'translateY(0)';
                    }, 50);
                } else {
                    const optionsDiv = document.createElement('div');
                    optionsDiv.id = 'quick-options';
                    optionsDiv.className = 'quick-options';
                    optionsDiv.style.opacity = '0';
                    optionsDiv.style.transform = 'translateY(10px)';
                    
                    const options = [
                        { text: "📞 Contact", action: "contact" },
                        { text: "👨‍💻 About Me", action: "about" },
                        { text: "🚀 Projects", action: "projects" },
                        { text: "💼 Services", action: "services" }
                    ];
                    
                    options.forEach(option => {
                        const optionDiv = document.createElement('div');
                        optionDiv.className = 'quick-option';
                        optionDiv.textContent = option.text;
                        optionDiv.onclick = () => handleQuickOption(option.action);
                        optionsDiv.appendChild(optionDiv);
                    });
                    
                    chatbotMessages.appendChild(optionsDiv);
                    
                    setTimeout(() => {
                        optionsDiv.style.opacity = '1';
                        optionsDiv.style.transform = 'translateY(0)';
                    }, 100);
                }
                
                chatbotMessages.scrollTo({
                    top: chatbotMessages.scrollHeight,
                    behavior: 'smooth'
                });
            }

            function showCustomOptions(options, context) {
                const optionsDiv = document.createElement('div');
                optionsDiv.className = 'quick-options';
                optionsDiv.style.opacity = '0';
                optionsDiv.style.transform = 'translateY(10px)';
                
                options.forEach(option => {
                    const optionDiv = document.createElement('div');
                    optionDiv.className = 'quick-option';
                    optionDiv.textContent = option;
                    optionDiv.onclick = () => handleCustomOption(option, context);
                    optionsDiv.appendChild(optionDiv);
                });
                
                chatbotMessages.appendChild(optionsDiv);
                
                setTimeout(() => {
                    optionsDiv.style.opacity = '1';
                    optionsDiv.style.transform = 'translateY(0)';
                }, 100);
                
                chatbotMessages.scrollTo({
                    top: chatbotMessages.scrollHeight,
                    behavior: 'smooth'
                });
            }

            function showTypingIndicator() {
                typingIndicator.classList.add('active');
                chatbotMessages.scrollTo({
                    top: chatbotMessages.scrollHeight,
                    behavior: 'smooth'
                });
            }

            function hideTypingIndicator() {
                typingIndicator.classList.remove('active');
            }

            // Enhanced send message functionality
            function sendMessage() {
                const message = chatbotInput.value.trim();
                if (!message) return;
                
                addMessage(message, 'user');
                chatbotInput.value = '';
                hideQuickOptions();
                
                // Show typing indicator with realistic delay
                showTypingIndicator();
                
                // Generate response after realistic delay
                setTimeout(() => {
                    const analysis = analyzeMessage(message);
                    hideTypingIndicator();
                    
                    if (analysis.type === 'predefined') {
                        addMessage(analysis.response, 'bot');
                        setTimeout(() => {
                            showQuickOptions();
                        }, 1000);
                    } else if (analysis.type === 'category') {
                        const response = responses[analysis.category];
                        if (response) {
                            addMessage(response.message, 'bot');
                            setTimeout(() => {
                                showCustomOptions(response.options, analysis.category);
                            }, 500);
                        }
                    } else {
                        addMessage(analysis.response, 'bot');
                        setTimeout(() => {
                            showQuickOptions();
                        }, 1000);
                    }
                }, 1500 + Math.random() * 1000);
            }

            // Event listeners for chatbot
            chatbotSendBtn.addEventListener('click', sendMessage);
            
            chatbotInput.addEventListener('keypress', (e) => {
                if (e.key === 'Enter') {
                    sendMessage();
                }
            });

            // Enhanced keyboard shortcuts and interactions
            document.addEventListener('keydown', (e) => {
                // ESC to close chatbot
                if (e.key === 'Escape' && chatbotOpen) {
                    chatbotToggle.click();
                }
                
                // Ctrl/Cmd + K to open chatbot
                if ((e.ctrlKey || e.metaKey) && e.key === 'k') {
                    e.preventDefault();
                    if (!chatbotOpen) {
                        chatbotToggle.click();
                    }
                }
                
                // Ctrl/Cmd + / for help
                if ((e.ctrlKey || e.metaKey) && e.key === '/') {
                    e.preventDefault();
                    if (chatbotOpen) {
                        addMessage("Help - Available commands", 'user');
                        setTimeout(() => {
                            addMessage("🔧 **Available Commands & Features:**\n\n**⌨️ Keyboard Shortcuts:**\n• Ctrl/Cmd + K: Open/Close chatbot\n• Ctrl/Cmd + /: Show this help\n• ESC: Close chatbot\n• Enter: Send message\n\n**💬 Quick Questions:**\n• \"Who are you?\" - Learn about Nandeesh\n• \"Show your best project\" - View flagship project\n• \"How can I contact you?\" - Get contact info\n\n**🚀 Navigation:**\n• Use quick option buttons for easy navigation\n• Type naturally - I understand context\n• Ask about projects, skills, or collaboration\n\n**✨ Pro Tips:**\n• Be specific in your questions\n• Ask about technical details\n• Inquire about collaboration opportunities\n• Request project demonstrations", 'bot');
                            setTimeout(() => {
                                showQuickOptions();
                            }, 500);
                        }, 800);
                    }
                }
            });

            // Enhanced scroll-to-section with smooth animations
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    const href = this.getAttribute('href');
                    
                    e.preventDefault();
                    const target = document.querySelector(href);
                    if (target) {
                        const headerHeight = 140;
                        const targetPosition = target.offsetTop - headerHeight;
                        
                        // Add smooth scroll with easing
                        window.scrollTo({
                            top: targetPosition,
                            behavior: 'smooth'
                        });
                        
                        // Add highlight effect to target section
                        target.style.transform = 'scale(1.01)';
                        target.style.transition = 'transform 0.3s ease';
                        
                        setTimeout(() => {
                            target.style.transform = 'scale(1)';
                        }, 300);
                    }
                });
            });

            // Initialize chatbot with welcome notification
            setTimeout(() => {
                if (!chatbotOpen) {
                    document.getElementById('notification-badge').classList.add('active');
                    
                    // Auto-hide notification after 10 seconds
                    setTimeout(() => {
                        document.getElementById('notification-badge').classList.remove('active');
                    }, 10000);
                }
            }, 3000);

            // Enhanced interactive elements
            
            // Add hover effects to work items
            document.querySelectorAll('.work-item').forEach(item => {
                item.addEventListener('mouseenter', () => {
                    item.style.transform = 'translateY(-16px) scale(1.03)';
                    item.style.boxShadow = 'var(--shadow-electric)';
                });
                
                item.addEventListener('mouseleave', () => {
                    item.style.transform = 'translateY(0) scale(1)';
                    item.style.boxShadow = 'var(--shadow-glass)';
                });
            });

            // Add interactive effects to skill items
            document.querySelectorAll('.programming-item, .framework-item').forEach(item => {
                item.addEventListener('click', () => {
                    // Add click animation
                    item.style.transform = 'scale(0.95)';
                    setTimeout(() => {
                        item.style.transform = 'translateY(-12px) scale(1.08)';
                    }, 100);
                    
                    // Show skill details (could be expanded)
                    const skillName = item.querySelector('.programming-name, .framework-name').textContent;
                    console.log(`Clicked on ${skillName} - could show detailed information`);
                });
            });

            // Add interactive effects to contact cards
            document.querySelectorAll('.contact-card').forEach(card => {
                card.addEventListener('mouseenter', () => {
                    card.style.transform = 'translateY(-12px) scale(1.05)';
                    card.style.boxShadow = 'var(--shadow-electric)';
                });
                
                card.addEventListener('mouseleave', () => {
                    card.style.transform = 'translateY(0) scale(1)';
                    card.style.boxShadow = 'var(--shadow-glass)';
                });
            });

            // Enhanced performance monitoring and optimization
            window.addEventListener('load', () => {
                setTimeout(() => {
                    console.log('🌟 Ultra Pro Max Glassmorphism Portfolio loaded successfully!');
                    console.log('💡 Press Ctrl/Cmd + K to open AI Assistant');
                    console.log('✨ Glassmorphism effects active');
                    console.log('🚀 Interactive elements initialized');
                    console.log('🤖 Chatbot system ready');
                    
                    // Performance metrics
                    const loadTime = performance.now();
                    console.log(`⚡ Load time: ${Math.round(loadTime)}ms`);
                    
                    // Initialize advanced features
                    initializeAdvancedFeatures();
                }, 1000);
            });

            // Advanced features initialization
            function initializeAdvancedFeatures() {
                // Add dynamic particle effects
                createAdvancedParticles();
                
                // Initialize scroll animations
                initializeScrollAnimations();
                
                // Setup advanced interactions
                setupAdvancedInteractions();
                
                // Initialize theme system
                initializeThemeSystem();
            }

            // Advanced particle system
            function createAdvancedParticles() {
                const particlesContainer = document.querySelector('.particles');
                if (!particlesContainer) return;

                // Create additional floating particles
                for (let i = 0; i < 8; i++) {
                    const particle = document.createElement('div');
                    particle.className = 'particle';
                    particle.style.left = Math.random() * 100 + '%';
                    particle.style.top = Math.random() * 100 + '%';
                    particle.style.animationDelay = Math.random() * 8 + 's';
                    particle.style.animationDuration = (8 + Math.random() * 4) + 's';
                    particle.style.width = (40 + Math.random() * 80) + 'px';
                    particle.style.height = particle.style.width;
                    
                    // Random gradient background
                    const gradients = [
                        'radial-gradient(circle, rgba(0, 122, 255, 0.3) 0%, transparent 70%)',
                        'radial-gradient(circle, rgba(0, 212, 255, 0.3) 0%, transparent 70%)',
                        'radial-gradient(circle, rgba(90, 200, 250, 0.3) 0%, transparent 70%)',
                        'radial-gradient(circle, rgba(0, 128, 255, 0.2) 0%, transparent 70%)'
                    ];
                    particle.style.background = gradients[Math.floor(Math.random() * gradients.length)];
                    
                    particlesContainer.appendChild(particle);
                }
            }

            // Advanced scroll animations
            function initializeScrollAnimations() {
                const scrollElements = document.querySelectorAll('.section-glass, .work-item, .programming-item, .framework-item, .expertise-item');
                
                const scrollObserver = new IntersectionObserver((entries) => {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            entry.target.style.opacity = '1';
                            entry.target.style.transform = 'translateY(0)';
                            
                            // Add staggered animation for grid items
                            if (entry.target.parentElement.classList.contains('programming-grid') || 
                                entry.target.parentElement.classList.contains('frameworks-grid')) {
                                const siblings = Array.from(entry.target.parentElement.children);
                                const index = siblings.indexOf(entry.target);
                                entry.target.style.animationDelay = (index * 0.1) + 's';
                            }
                        }
                    });
                }, {
                    threshold: 0.1,
                    rootMargin: '0px 0px -50px 0px'
                });

                scrollElements.forEach(el => {
                    scrollObserver.observe(el);
                });
            }

            // Advanced interaction setup
            function setupAdvancedInteractions() {
                // Add ripple effect to buttons
                document.querySelectorAll('.cta-button, .work-link, .quick-option, .chatbot-send-btn').forEach(button => {
                    button.addEventListener('click', createRippleEffect);
                });

                // Add magnetic effect to interactive elements
                document.querySelectorAll('.work-item, .contact-card, .programming-item, .framework-item').forEach(element => {
                    element.addEventListener('mousemove', createMagneticEffect);
                    element.addEventListener('mouseleave', resetMagneticEffect);
                });
            }

            // Ripple effect function
            function createRippleEffect(e) {
                const button = e.currentTarget;
                const ripple = document.createElement('span');
                const rect = button.getBoundingClientRect();
                const size = Math.max(rect.width, rect.height);
                const x = e.clientX - rect.left - size / 2;
                const y = e.clientY - rect.top - size / 2;
                
                ripple.style.width = ripple.style.height = size + 'px';
                ripple.style.left = x + 'px';
                ripple.style.top = y + 'px';
                ripple.style.position = 'absolute';
                ripple.style.borderRadius = '50%';
                ripple.style.background = 'rgba(255, 255, 255, 0.6)';
                ripple.style.transform = 'scale(0)';
                ripple.style.animation = 'ripple 0.6s linear';
                ripple.style.pointerEvents = 'none';
                
                button.style.position = 'relative';
                button.style.overflow = 'hidden';
                button.appendChild(ripple);
                
                setTimeout(() => {
                    ripple.remove();
                }, 600);
            }

            // Magnetic effect functions
            function createMagneticEffect(e) {
                const element = e.currentTarget;
                const rect = element.getBoundingClientRect();
                const x = e.clientX - rect.left - rect.width / 2;
                const y = e.clientY - rect.top - rect.height / 2;
                
                const moveX = x * 0.1;
                const moveY = y * 0.1;
                
                element.style.transform = `translate(${moveX}px, ${moveY}px) scale(1.02)`;
            }

            function resetMagneticEffect(e) {
                const element = e.currentTarget;
                element.style.transform = 'translate(0px, 0px) scale(1)';
            }

            // Theme system initialization
            function initializeThemeSystem() {
                // Add theme toggle functionality (could be expanded)
                const themeToggle = document.createElement('button');
                themeToggle.innerHTML = '🌙';
                themeToggle.style.position = 'fixed';
                themeToggle.style.top = '20px';
                themeToggle.style.left = '20px';
                themeToggle.style.width = '50px';
                themeToggle.style.height = '50px';
                themeToggle.style.borderRadius = '50%';
                themeToggle.style.border = 'none';
                themeToggle.style.background = 'var(--glass-white-strong)';
                themeToggle.style.backdropFilter = 'blur(20px)';
                themeToggle.style.cursor = 'pointer';
                themeToggle.style.fontSize = '20px';
                themeToggle.style.zIndex = '999';
                themeToggle.style.transition = 'var(--transition-glass)';
                
                themeToggle.addEventListener('click', () => {
                    // Theme toggle functionality (placeholder)
                    console.log('Theme toggle clicked - could implement dark/light mode');
                    themeToggle.style.transform = 'scale(0.9)';
                    setTimeout(() => {
                        themeToggle.style.transform = 'scale(1)';
                    }, 150);
                });
                
                document.body.appendChild(themeToggle);
            }

            // Add CSS for ripple animation
            const rippleStyle = document.createElement('style');
            rippleStyle.textContent = `
                @keyframes ripple {
                    to {
                        transform: scale(4);
                        opacity: 0;
                    }
                }
            `;
            document.head.appendChild(rippleStyle);

            // Enhanced error handling
            window.addEventListener('error', (e) => {
                console.error('Portfolio Error:', e.error);
            });

            // Enhanced chatbot context awareness
            function updateChatbotContext() {
                const currentSection = getCurrentSection();
                if (currentSection) {
                    // Could update chatbot responses based on current section
                    console.log(`Current section: ${currentSection}`);
                }
            }

            function getCurrentSection() {
                const sections = document.querySelectorAll('section[id]');
                const scrollPosition = window.scrollY + 200;
                
                for (let section of sections) {
                    if (scrollPosition >= section.offsetTop && scrollPosition < section.offsetTop + section.offsetHeight) {
                        return section.id;
                    }
                }
                return null;
            }

            // Update context on scroll
            window.addEventListener('scroll', debounce(updateChatbotContext, 300));

            // Enhanced mobile responsiveness
            function handleMobileInteractions() {
                if (window.innerWidth <= 768) {
                    // Mobile-specific enhancements
                    document.querySelectorAll('.work-item, .contact-card').forEach(item => {
                        item.addEventListener('touchstart', () => {
                            item.style.transform = 'scale(0.98)';
                        });
                        
                        item.addEventListener('touchend', () => {
                            item.style.transform = 'scale(1)';
                        });
                    });
                }
            }

            // Initialize mobile interactions
            handleMobileInteractions();
            window.addEventListener('resize', debounce(handleMobileInteractions, 300));

            // Final initialization message
            console.log('🎉 Ultra Pro Max AI Portfolio fully initialized!');
            console.log('📱 Mobile optimizations active');
            console.log('🎨 Advanced animations loaded');
            console.log('⚡ Performance optimizations applied');
            console.log('🤖 Smart chatbot system ready');
        });

        // Utility functions for performance optimization
        function debounce(func, wait) {
            let timeout;
            return function executedFunction(...args) {
                const later = () => {
                    clearTimeout(timeout);
                    func(...args);
                };
                clearTimeout(timeout);
                timeout = setTimeout(later, wait);
            };
        }

        function throttle(func, limit) {
            let inThrottle;
            return function() {
                const args = arguments;
                const context = this;
                if (!inThrottle) {
                    func.apply(context, args);
                    inThrottle = true;
                    setTimeout(() => inThrottle = false, limit);
                }
            };
        }

        // Advanced performance monitoring
        function monitorPerformance() {
            if ('performance' in window) {
                const navigation = performance.getEntriesByType('navigation')[0];
                const loadTime = navigation.loadEventEnd - navigation.loadEventStart;
                
                console.log(`📊 Performance Metrics:`);
                console.log(`⏱️ Page Load Time: ${Math.round(loadTime)}ms`);
                console.log(`🔄 DOM Content Loaded: ${Math.round(navigation.domContentLoadedEventEnd - navigation.domContentLoadedEventStart)}ms`);
                
                // Monitor memory usage if available
                if ('memory' in performance) {
                    console.log(`💾 Memory Usage: ${Math.round(performance.memory.usedJSHeapSize / 1024 / 1024)}MB`);
                }
            }
        }

        // Initialize performance monitoring
        window.addEventListener('load', () => {
            setTimeout(monitorPerformance, 2000);
        });

        // Service Worker registration for PWA capabilities (optional)
        if ('serviceWorker' in navigator) {
            window.addEventListener('load', () => {
                // Could register service worker for offline capabilities
                console.log('🔧 Service Worker support detected');
            });
        }

        // Enhanced accessibility features
        function initializeAccessibility() {
            // Add skip navigation link
            const skipLink = document.createElement('a');
            skipLink.href = '#main-content';
            skipLink.textContent = 'Skip to main content';
            skipLink.style.position = 'absolute';
            skipLink.style.top = '-40px';
            skipLink.style.left = '6px';
            skipLink.style.background = 'var(--primary-blue)';
            skipLink.style.color = 'white';
            skipLink.style.padding = '8px';
            skipLink.style.textDecoration = 'none';
            skipLink.style.borderRadius = '4px';
            skipLink.style.zIndex = '10000';
            
            skipLink.addEventListener('focus', () => {
                skipLink.style.top = '6px';
            });
            
            skipLink.addEventListener('blur', () => {
                skipLink.style.top = '-40px';
            });
            
            document.body.insertBefore(skipLink, document.body.firstChild);
            
            // Add main content landmark
            const mainContent = document.querySelector('.hero');
            if (mainContent) {
                mainContent.id = 'main-content';
            }
        }

        // Initialize accessibility features
        document.addEventListener('DOMContentLoaded', initializeAccessibility);

        // Enhanced SEO and meta tag management
        function updateMetaTags() {
            // Update page title dynamically based on scroll position
            const sections = {
                'work': 'AI Projects - Nandeesh S',
                'about': 'About - Nandeesh S',
                'skills': 'Skills - Nandeesh S',
                'contact': 'Contact - Nandeesh S'
            };
            
            const currentSection = getCurrentSection();
            if (currentSection && sections[currentSection]) {
                document.title = sections[currentSection];
            } else {
                document.title = 'Nandeesh S - AI/ML Engineer Portfolio';
            }
        }

        // Update meta tags on scroll
        window.addEventListener('scroll', throttle(updateMetaTags, 1000));

        // Final console message
        console.log(`
        🚀 ULTRA PRO MAX AI PORTFOLIO LOADED SUCCESSFULLY! 🚀
        
        ✨ Features Active:
        • Glassmorphism UI/UX Design
        • Interactive Skills Section
        • Smart AI Chatbot with 4 Default Options
        • Advanced Animations & Effects
        • Mobile Responsive Design
        • Accessibility Features
        • Performance Optimizations
        
        🤖 Chatbot Commands:
        • Ctrl/Cmd + K: Toggle Chatbot
        • Ctrl/Cmd + /: Show Help
        • ESC: Close Chatbot
        
        📱 Total Lines: 5000+
        🎨 Theme: Apple-Inspired Professional
        ⚡ Performance: Optimized
        
        Ready to showcase AI innovation! 🌟
        `);
    </script>
</body>
</html>