
## How to Run (Mac)

```bash
# Install PHP if not already installed
brew install php

# Start PHP built-in server from this folder
php -S localhost:8000

# Open browser and go to:
# http://localhost:8000/index.html
```

> Note: The `uploads/` folder is created automatically on first successful upload.